# 游泳赛事管理与计时系统 使用说明书

**版本**：v2026.05.26
**适用平台**：Windows 10 / 11（x64）
**主要分发文件**：`游泳赛事管理系统_安装包_<日期>.zip` / `SwimmingTimingSystem-Setup-v<日期>.zip`

---

## 本版主要变更（v2026.05.20 → v2026.05.26 汇总）

本期重点：**比赛控制状态机三个回归 + 50m 比赛 PC 协议解读纠正 + 设备状态全套持久化 + 赛事概览面板改造**。

### 一、比赛控制三个关键回归修复

#### 1.1 泳道封闭倒计时到 0 后设备不打开（必修）

**现象**：开机后选项目 → 准备就绪 → M.发令 → 泳道关闭时间倒计时到 0，但触板 / 盲表都没自动打开。

**根因**：`CountdownTimer_Tick` 用 `state.LaneCloseCountdown > 0` 作为外层 gate；显示用 `Math.Round(remaining*10)/10` 在 `remaining<0.05` 时会把 `LaneCloseCountdown` 提前四舍五入为 0 → 下次 tick 外层 gate 失败 → "开设备"分支永远不进。

**修复**：gate 改用真正的时间锚 `CountdownStartedAt` + `CountdownTargetSec`，`LaneCloseCountdown` 降级为纯 UI 显示值。倒计时跨过 0 后清空锚点防止重复打开。

#### 1.2 LaneCloseTime 设 10s 但实际倒计时 20s

**现象**：参数设置里"泳道关闭时间"改成 10 秒并保存，但发令后倒计时从 20 秒开始。

**根因**：`LaneDeviceState._laneCloseTime` 默认 20.0，而 `StartRace_Click` 用 `state.LaneCloseTime > 0 ? state.LaneCloseTime : 全局`。`20 > 0` 永远成立，每泳道默认值压全局值。

**修复**：`LaneDeviceState._laneCloseTime` 默认改为 `0.0`（= "用全局"语义）。Per-lane 非零值仅由硬件 / 远程台明确写入时生效。

#### 1.3 跳过"计时复位"直接按"准备就绪"参数残留

**现象**：用户跳过计时复位直接按准备就绪后，旧组的滚动时间 / leader 显示 / 分段跟踪 / 硬件状态都残留。

**修复**：`EnterReadyStateInternal` 入口补"复位类清场"段（从 Restart_Click 移植）：
- 停 `_raceTimer` / `_countdownTimer`
- 清 `_runningTime` / `_hwRunningTime*` / `_firstPlaceShowStart` / `_lastLeaderSplitCount`
- 清 4 个分段反应时跟踪字典 + `_rawTimingLog`
- 硬件发 `0x20` + `0x7F` + 重发 `Set_MatchEvent`
- **不**删 `Swimmer.Results`（那是 Restart 的"丢弃成绩"语义，Ready 路径不应碰已有成绩）

行为：现在不管用户走 复位 → 就绪、直接就绪、或者多次切换，PC 和硬件都进入干净状态再开始。

### 二、50m 比赛 PC 端协议解读纠正

**现象**：PC 选 50m 项目后，硬件 LED 发令点 + 圈数正确，但出发台开错端、游泳方向错、后续触板/盲表也错位。硬件 +1/-1 按键设置 50m 完全正确。

**根因**：`0x41 Set_ArmDelay_Time` 帧 d8 bit 0 在硬件代码里叫 `FinalPlace`（终点端，0=左/1=右），PC 端之前误当 `StartPlace`（起点端）发送。
- 100m+ 比赛 `sp50m=0` → `StartPosition == FinishPosition` → 两个字段值相同 → 编码一样 → 误打误撞对了
- 50m 比赛 `sp50m=1` → `Start ≠ Finish` → 编码不一样 → 出发台开错端

**修复**：d8 bit 0 改发 `FinishPosition`：
```csharp
byte finalPl = (FinishPosition == "right" ? 1 : 0);
byte placeDir = (laneDir << 4) | (sp50m << 1) | finalPl;
```

**注意**：硬件端**不需要任何改动** —— 是 PC 端协议字段误读，硬件代码本来就一直读 FinalPlace。

### 三、SetCurrentEvent / SetCurrentHeat 切换时补发 0x41

**配套修复**：之前 PC 切项目/切组次时只发 `0x43` + `0x10 0x42`，漏发 `0x41`。硬件就用上一帧的 sp50m 处理。
- 现已在 `SetCurrentEvent`（line 5294）和 `SetCurrentHeat`（line 5443）的硬件同步块都补发 `SendTimingSettingsToHardware()`（0x41 + 0x44 + 0x45），保证每次切项目/组次都更新 sp50m 位。

### 四、滚动时间显示"只有 1 名运动员触板才停留"

**镜像 display.html `_dspFirstDetected` 逻辑**：
- 1 名判定：count desc（finished=9999）+ rank 小并列 + lastCum 兜底
- 触发条件：触板的那道 == 1 名所在道
- 显示：1 名运动员自己的最新累计时间
- 停留时长：`FirstPlaceHoldTime`（参数"第 1 名成绩显示时间"，display.html 也同步用此参数）
- 2/3 名追触板不动 `_firstPlaceShowStart`，让滚动时间继续 / 保持上次 1 名时间

**配套**：移除 `ProcessTimingData` 0x7F 分支对 `RunningTimeText.Text` 的直接覆盖（与 `RaceTimer_Tick` 双写打架）。UI 写入收敛到 `RaceTimer_Tick` 单一来源。

### 五、display 端每泳道分段成绩切组后不显示

**根因**：`laneDisplayState` 只在 `rState==='waiting'` 时清空。比赛切组常走 Finished→Ready→Racing 不经过 Waiting，残留的 `splitCount`（比如 200m=4 段）卡死新组判断：`realSplitCount(1) > ls.splitCount(4)` 为假 → `showTime` 不更新 → 分段成绩永远进不了 5 秒显示窗口。

**修复**：display.html `_ensureRaceRowsDom` 重建 DOM 时（heat / event / 运动员任一变化）整体清空 `laneDisplayState`，与主控 `Restart_Click` 清 `_laneSplitCount` 对称。

### 六、发令位置变化时左右指示灯不刷新

**根因**：
1. `UpdateLaneStatusDisplay` 的 UI 重建 key 没包含 `StartPosition` → 50m 自动切对岸 / 参数面板改发令位置时 `RenderPoolHeader` 不重建 → 顶部绿色发令位置徽章不刷新。
2. `RefreshLaneRows` 里占用泳道每 tick 刷新 `LeftSignalInd/RightSignalInd`，空泳道的行 `continue` 跳过了 → 空道指示灯停在 `BuildLaneRows` 初值。

**修复**：
1. UI key 加 `|sp:{StartPosition}`
2. 空泳道分支也设置 `LeftSignalInd.Background / RightSignalInd.Background`

### 七、设备状态全套持久化

**之前**：`Left/RightBlindWatch{1,2,3}NotInstalled` 是 `ApplyTouchpadInstallModeToLanes` 按 `HasRightStartBlock + FinishPosition` 算出来的"计算态"，但 `device_states.json` 只持久化盲表 6 个 NotInstalled，且 `HasRightStartBlock` 完全没存盘 → 重启后 `HasRightStartBlock` 回到默认 true，老 JSON 里残留的"单端时算出的左侧 NotInstalled=true"被读回 → 开机左侧盲表显示未安装。

**修复**（按 "所有参数 + 计算后状态全部存盘，开机初始化 + 读盘覆盖" 思路）：

1. **`LaneCloseSettings` 新增 `HasRightStartBlock`** 属性，进 `timing_settings.json` 持久化；`LoadTimingSettings` 装载后同步回 `_poolConfig.HasRightStartBlock`。
2. **`device_states.json` 扩到 12 个 NotInstalled 标志**：触板 2 + 出发台 2 + 盲表 6（之前只 6 个）。加上原 10 个 Broken + 2 个 ManualEnabled = 24 字段/道。
3. **3 处改 HasRightStartBlock 的入口**（远程台 / 硬件 0x3A / 设置对话框）都同步写 `_laneCloseSettings.HasRightStartBlock` + `SaveDeviceStates` + `SaveTimingSettings`。
4. **硬件 0x63 终点位置上报**同步 `SaveDeviceStates` + `SaveTimingSettings`。
5. **盲表数量对话框** "点确认就存"（去掉 `if (changed)` 优化）。

### 八、赛事概览面板改造

#### 8.1 仲裁委员字段 + 删除发令员 / 编排长

- 增加"仲裁委员"字段（多人逗号分隔）
- 删除"发令员"和"编排长"两行
- 技术代表 / 仲裁委员 / 总裁判长 排同一行（网格扩到 6 列）
- "参赛单位管理"、"工作人员管理"按钮移到"新建赛事"右边

#### 8.2 编排长签字位置全部删除

历史代码里"裁判长签字"那行实际填的是 `ChiefJudgeBox`（即编排长数据），全部删除：
- 通用文档底部签名行（`DocSignatureRow`）
- `EventResultPrintWindow` 签名栏
- 秩序册"竞赛人员"表（仲裁委员改 `colspan='3'` 独占一行）
- 奖状证书底部
- 破纪录证书底部

`ChiefJudgeBox` 保留为隐藏控件以兼容 `.cs` 引用。

### 九、接力队 AgeGroup 字段全链路

- `RelayTeam.AgeGroup` 新增
- 建队 / 编辑接力 / CSV 导出 / 秩序册按组别分组都加 AgeGroup
- 接力队唯一键由"队名+性别+项目" → "队名+性别+项目+组别" 四键

### 十、秩序册生成向导多天平铺

- 进 Tab3 时按"赛事概览"开始/结束日期生成完整日期列表（原仅塞 `DateTime.Today`）
- "自动"按钮多天平铺：240 分钟/时段满即滚下一天，不再全堆到当天
- 一键全自动默认起始日期/天数从赛事概览推断

### 十一、报名 register.html 改造

- 个人项目报名 / 接力项目报名 改为顶部 tab 切换布局
- **必填字段**：姓名 / 出生日期 / 身份证号(15 或 18 位) / 代表队 / 组别 / 性别
- 接力按队名查询 → 回填修改
- 接力队组别必填，与个人组别同一下拉源

### 十二、display 端滚动时间停留时长统一

- display.html `_dspFirstHoldSec` 从 `splitDisplayTime` 改成 `firstPlaceHoldTime`，和主控 `RaceTimer_Tick` 同一参数。"分段成绩显示时长" 和 "第 1 名成绩显示时间"语义彻底分开。

---

## 历史变更（v2026.05.13 → v2026.05.20 汇总）

本期以"硬件通讯协议大改 + 套件化重构 + 三端协同细化"为主线。

### 一、硬件计时器通讯协议大幅扩展

参见 `docs/通讯协议变更说明_v2026.05.20.txt`（权威协议规范）。

**新增 6 条命令**：

| 命令 | 方向 | 用途 | 引入 |
|---|---|---|---|
| `0x60` Set_LaneEnableDisable | PC → 硬件 | 单道屏蔽 / 启用（同时切换道次按钮显示） | v2026.05.17 |
| `0x61` Set_LapRemaining | PC → 硬件 | 剩余圈数 ±1 调整后下发硬件 | v2026.05.17 |
| `0x62` Set_LaneOrder | **双向** | 道次顺序（正序 0→9 / 逆序 9→0）切换 | v2026.05.17 |
| `0x63` Set_FinishPosition | **双向** | 终点位置（左 / 右）切换 | v2026.05.17 |
| `0x64` Set_TimingsBundle | 硬件 → PC | 硬件 net_test 退出时一帧打包上报 5 项时间 | v2026.05.17 |
| `0x65` Set_RefreshDisplay | PC → 硬件 | 参数对话框 OK 后让硬件主控按新值重画一屏 | v2026.05.18 |

**扩展原有 3 条命令为双向**：`0x3A` Set_PoolSingleOrDoubleTP、`0x47` Set_LaneOpenClose（v2026.05.16），并新增 `0x44` 作为 `0x40` 的别名。

**周期校时**：连接硬件后立即下发 `0x39 SetDateTime`，之后每 30 分钟刷一次，避免长时间比赛硬件 RTC 漂移。

**lane 索引按"屏幕位置"编码**（v2026.05.18）：所有 PC→硬件帧（0x43 道次位图 / 0x60 / 0x61 / 0x47 等）的 lane 字段都先经过 `LaneToHwIndex(lane)`——逆序模式下 `hwIndex = 9 − lane`；硬件 UI 按行排列，PC 端按实际泳道号操作，二者通过该映射对齐。

**单端 / 两端触板模式**：参数设置新增"泳池触板"单选（单边 / 两端）；切换后 `0x3A Set_PoolSingleOrDoubleTP` 下发硬件，硬件 SD 卡持久化。终点位置（左 / 右）变化时自动联动刷新各道 `NotInstalled` 标记。

### 二、按钮命名统一（v2026.05.19）

主服务器、EXE 远程台、HTML 计时台、硬件主控四端按钮文字打通：

| 旧 | 新 |
|---|---|
| 就位 | **准备就绪** |
| 发令 / 开始计时 | **M.发令** |
| 复位 / 重置 / 计时器清零 | **计时复位** |

### 三、顶部"硬件计时器"连接状态指示（v2026.05.19）

主服务器顶部状态栏右侧新增红/绿圆点 + "硬件计时器: 已连接 / 未连接"文字（粗体），由 `UpdateConnectionStatus()` 驱动；EXE 远程台顶栏同款指示灯。

### 四、系统硬件栏（v2026.05.14 起，v2026.05.19 整合）

主服务器 "比赛控制" Tab 右栏新增"系统硬件"分区，含 2 个按钮 + 1 个状态显示：

- **网络连接**（绿 `#22C55E`）：用 `timing_connection.json` 中保存的 TCP 主机/端口快速连接；已连接时变红"断开"。
- **设备测试**（青 `#0EA5E9`）：发送 `0x1D` 给硬件，所有非损坏 / 非未安装设备置 Open，硬件来什么数据都接收显示，便于联调；再点退出。
- **电池电压**：接收硬件 `0x4B BatteryVoltage` 帧实时显示（如 "7.42 V"），30 秒无更新自动置 "—"。

HTML / EXE 远程台同样在顶 nav 提供这 3 个控件。

### 五、参数设置增强

- **道次顺序** 单选（正序 0→9 / 逆序 9→0）：写入 `timing_settings.json`，下发硬件 `0x62`，三端 UI 同时按选定顺序排列。
- **盲表代替成绩延迟** 单位统一为**整数秒（0–9）**（v2026.05.18-4），与硬件 `0x64` D6 字段对齐；硬件内部仍乘 10 转换为 0.1 秒精度。
- **进出对话框保存恢复**（v2026.05.18-2）：进入对话框时先快照各道 `IsFinished` / `LeftLapManualAdjust` / `RightLapManualAdjust`，按"确定"应用参数后再恢复——避免参数微调误清零操作员手调的剩余圈数；按"取消"则一律放弃。OK 后下发 `0x65 SetRefreshDisplay` 让硬件按新参数重画。
- **终点位置变化**：自动触发 `ApplyTouchpadInstallModeToLanes()` 刷新各道 NotInstalled 标记（v2026.05.18-3）。

### 六、设备状态持久化 + 设备五态

`device_states.json`（同 EXE 目录）保存设备配置类字段：触板 / 出发台 / 盲表 1-3 的 `*Broken` × 左/右、盲表 1-3 的 `*NotInstalled` × 左/右、`LeftManualEnabled` / `RightManualEnabled`。

设备点五态颜色：

| 状态 | 颜色 | 含义 |
|---|---|---|
| Open 接收中 | 🟢 绿 `#22C55E` | 设备可正常上报 |
| Touched 已触板 | 🔴 红 `#EF4444` | 出过正式成绩后窗口期内 |
| Broken 损坏 | ⚫ 黑 `#000000` | 设备状态管理标黑 |
| FalseStart 抢跳 | 🟡 黄 `#F59E0B` | 反应时低于阈值 |
| Closed 关闭 | ⬜ 灰 `#475569` | 当前不接收信号 |
| NotInstalled 未安装 | 虚线框 | 单边模式下"非终点端"自动标 |

### 七、套件化登录：四个 EXE 共享 Suite 风格深色登录窗

- **SwimmingScoreboard**（主服务器）
- **RemoteTimingControl**（远程计时控制台）
- **RegistrationTool**（注册工具）
- **RemoteDisplayControl**（远程显示控制）
- **ScheduleEditor**（新增，见下节）

所有登录窗背景 `#1E293B`（slate-900），统一字段（用户名 / 密码 / 记住登录名和密码）+ "登  录" 按钮（`#3B82F6`），回车提交；首次启动 admin/admin。凭据 SHA256(salt+password)，"记住"功能用 Windows DPAPI 加密密码，分别落到各自目录的 `*_credentials.json` + `*_remember.json`。主界面右上角有"修改用户名和密码"按钮。

### 八、ScheduleEditor — 编排记录及成绩处理 EXE（新增）

`ScheduleEditor.exe` 是 SwimmingScoreboard 主程序的"轻量化"复用：通过 `<Page Include="..\SwimmingScoreboard\MainWindow.xaml" Link="..."/>` 链接，运行时检测入口程序集名 `IsScheduleEditorMode = (Assembly.GetEntryAssembly().GetName().Name == "ScheduleEditor")`，自动：

- **只显示 3 个 Tab**：赛事管理与报名 / 成绩与排名 / 文档编辑/输出/打印
- 隐藏其它 4 个 Tab（系统工作状态 / 比赛控制 / 系统日志与数据 / 设置）
- 跳过 `InitializeWebSocketServer()` 与 `InitializeTimingBridge()`——不连硬件、不开 3002 端口
- 顶栏注入 `InjectEditorSyncToolbar()`：替换"控制模式: 本地"为"主服务器: [IP] [连接] [状态]"
- 通过 `EditorSyncClient` 通过 WebSocket 连主服务器 3002，按 `EDITOR_IDENTITY` / `EDITOR_PUSH_PACKAGE` / `EDITOR_PACKAGE` 协议进行 CompetitionPackage 整包双向同步——主服务器自动保存就推给所有编排端；编排端保存也推回主服务器，从而实现"一个比赛、多个工位编排"协同。

适用场景：赛事编排员、成绩录入员、文档制作员可在不参与硬件计时的另一台 PC 上同时工作。

### 九、电池电压阈值按 7.4V 实际包对齐

硬件电池配的 2S Li-ion 包标称 7.4 V，主控显示 `BatteryVoltage = (D3<<8 | D4) / 1000.0` V：

| 区间 | 颜色 | 含义 |
|---|---|---|
| ≥ 7.4 V | 绿 | 电量充足 |
| 6.8 ~ 7.4 V | 黄 | 电量警告 |
| < 6.8 V | 红 | 电量低，建议充电 |

> 此前版本（v2026.04 / v2026.05.10）的圈数 spinner、接力 4 棒反应时全链路、纪录自动比对、并列名次、起跳犯规人工判罚、隐私字段保护等改动仍然有效，详见后续章节。

---

## 目录

1. [系统概述](#1-系统概述)
2. [安装与启动](#2-安装与启动)
3. [系统架构与通讯](#3-系统架构与通讯)
4. [硬件通讯协议（命令字节表）](#4-硬件通讯协议命令字节表)
5. [比赛参数设置管理](#5-比赛参数设置管理)
6. [赛事管理与报名](#6-赛事管理与报名)
7. [运动员管理](#7-运动员管理)
8. [接力队管理](#8-接力队管理)
9. [赛程与分组](#9-赛程与分组)
10. [比赛控制](#10-比赛控制)
11. [成绩与排名](#11-成绩与排名)
12. [纪录管理](#12-纪录管理)
13. [文档编辑 / 输出 / 打印](#13-文档编辑--输出--打印)
14. [显示控制（大屏 / 排名屏 / 媒体）](#14-显示控制大屏--排名屏--媒体)
15. [系统日志与数据管理](#15-系统日志与数据管理)
16. [参数设置与硬件连接](#16-参数设置与硬件连接)
17. [RemoteTimingControl EXE](#17-remotetimingcontrol-exe)
18. [HTML 浏览器计时控制台](#18-html-浏览器计时控制台)
19. [ScheduleEditor 编排记录及成绩处理 EXE](#19-scheduleeditor-编排记录及成绩处理-exe)
20. [RegistrationTool / RemoteDisplayControl](#20-registrationtool--remotedisplaycontrol)
21. [网页检录台](#21-网页检录台)
22. [大屏显示 / 排名屏](#22-大屏显示--排名屏)
23. [在线报名 / 注册工具](#23-在线报名--注册工具)
24. [自动化测试机器人](#24-自动化测试机器人)
25. [硬件参数调试机器人](#25-硬件参数调试机器人)
26. [常见问题](#26-常见问题)
27. [附录 A：CSV 表头速查](#附录-acsv-表头速查)
28. [附录 B：持久化文件清单](#附录-b持久化文件清单)
29. [附录 C：版本变更摘要](#附录-c版本变更摘要)

---

## 1. 系统概述

### 1.1 设计目标

本系统是一套**完整的游泳比赛管理与计时解决方案**，覆盖赛事全生命周期：

- **赛前**：赛事创建 / 运动员报名 / 自动或手动分组 / 秩序册编辑 / 纪录数据导入
- **赛中**：硬件计时实时联动 / 三端控制台同步 / 设备状态管理 / 大屏展示 / 媒体播放
- **赛后**：成绩确认与排名 / 多轮晋级 / 成绩册编辑 / 多格式文档输出（PDF / DOC / HTML）

系统遵循 **World Aquatics（FINA）SW 3 竞赛规则** 自动分组（循环 / 直接排位 + 矛头泳道分配）。

### 1.2 设计原则：服务器是单一数据源

所有大屏、排名屏、文档（编辑 / 输出 / 打印）显示的数据**都来自比赛控制端（主服务器）**。前端浏览器 / 打印模板**直接渲染服务器广播的字段**（`sw.rank`、`sw.reactionTime`、`sw.legReactionTimes`、`sw.recordNote` 等），**不再做二次重排或重算**。

> 任何"大屏显示某字段不对"、"打印某列不对"的问题，都先回到主服务器的记录 / 计算路径去修，让广播本身的数据正确，再让前端原样渲染。前端纯展示层格式化（小数位、HTML 转义、奖牌色映射）仍可在前端做。

### 1.3 系统组成

| 组件 | 说明 | 运行方式 |
|---|---|---|
| **主服务器** `SwimmingScoreboard.exe` | 赛事管理核心，连接计时硬件，承载所有数据 | Windows 应用 |
| **远程计时控制台** `RemoteTimingControl.exe` | 异机比赛控制客户端 | Windows 应用 |
| **HTML 计时控制台** `race_control.html` | 浏览器端比赛控制（与 EXE 等价） | 浏览器全屏 |
| **编排记录及成绩处理** `ScheduleEditor.exe` | 编排员 / 成绩员 / 文档员 协同工位 | Windows 应用 |
| **网页检录台** `checkin.html` | 检录台核对身份、登记 DSQ/DNS/DNF | 浏览器 |
| **大屏显示** `display.html` | 观众大屏（项目 / 成绩 / 纪录 / 媒体） | 浏览器全屏 |
| **排名屏** `leaderboard.html` | 项目 / 团体 / 纪录排名展示 | 浏览器全屏 |
| **显示控制（网页）** `control.html` | 大屏显示模式遥控 | 浏览器 |
| **显示控制（桌面）** `RemoteDisplayControl.exe` | 桌面版大屏显示模式遥控 | Windows 应用 |
| **在线报名** `register.html` | 运动员 / 接力队自助报名 | 浏览器 |
| **桌面注册工具** `RegistrationTool.exe` | 桌面端报名 | Windows 应用 |
| **自动化测试机器人** `test_bot.html` | 模拟报名 / 比赛流程，便于演示与压测 | 浏览器 |
| **计时模拟器** `TimingSimulator.exe` | 无硬件时调试通讯协议 | Windows 应用 |
| **硬件参数调试机器人** `ParamDebugBot.exe` | 与硬件计时器双向调试 | Windows 应用 |

### 1.4 系统要求

- **操作系统**：Windows 10 / 11（推荐 64 位）
- **运行环境**：.NET Framework 4.5+（Windows 通常已预装）
- **网络**：局域网即可（主服务器与各客户端、大屏在同一子网）
- **端口**：`3002`（WebSocket，所有客户端共用）
- **浏览器**：Chrome / 新版 Edge（推荐最新稳定版）
- **硬件计时器**：通过 TCP / UDP / 串口 任一方式连接（协议详见 §4）

---

## 2. 安装与启动

### 2.1 安装

1. 解压 `SwimmingTimingSystem-Setup-v<日期>.zip`
2. 双击 `Setup.exe`，选择安装目录（默认 `C:\SwimmingTimingSystem`）
3. 安装完成，桌面 / 开始菜单生成各 EXE 快捷方式

### 2.2 桌面 EXE 通用登录流程

四个桌面 EXE（**SwimmingScoreboard / RemoteTimingControl / ScheduleEditor / RegistrationTool / RemoteDisplayControl**）共享 Suite 风格深色登录窗：

- 窗口背景 `#1E293B`，标题分别为：
  - "游泳赛事管理系统 — 主控"（SwimmingScoreboard）
  - "游泳赛事管理系统 — 比赛控制"（RemoteTimingControl）
  - "游泳赛事管理系统 — 编排记录及成绩处理"（ScheduleEditor）
  - "游泳赛事管理系统 — 运动员报名"（RegistrationTool）
  - "游泳赛事管理系统 — 显示控制"（RemoteDisplayControl）
- 字段：**用户名** / **密码** / "记住登录名和密码" 复选框 / **登 录** 按钮
- 回车键提交
- **首次启动密码 admin / admin**——登录后请立即到主界面右上角"修改用户名和密码"重置

凭据持久化在 EXE 同目录：

| EXE | 凭据文件 | 记住文件 |
|---|---|---|
| RemoteTimingControl | `timing_credentials.json` | `timing_remember.json` |
| RegistrationTool | `register_credentials.json` | `register_remember.json` |
| RemoteDisplayControl | `display_credentials.json` | `display_remember.json` |
| ScheduleEditor | `editor_credentials.json` | `editor_remember.json` |

密码哈希 SHA256(salt+password)，"记住"功能用 Windows DPAPI（`DataProtectionScope.CurrentUser`）加密——换 Windows 账户读不到，安全性可接受。

### 2.3 启动主服务器

1. 双击 **"游泳赛事管理主服务器"**（`SwimmingScoreboard.exe`）
2. 登录后进入主界面
3. 主界面 7 个 Tab：系统工作状态 / 赛事管理与报名 / 比赛控制 / 成绩与排名 / 文档编辑/输出/打印 / 系统日志与数据 / 设置

### 2.4 启动其它桌面 EXE（异机操作）

| EXE | 登录后主界面 |
|---|---|
| RemoteTimingControl | 顶栏服务器 IP + 连接按钮 + 硬件计时器指示灯 + 系统硬件 3 按钮（连接串口 / 设备测试 / 设备全开）+ 参数设置 + 修改密码；主体 3 列（赛程导航 + 泳道实时状态 + 比赛控制） |
| ScheduleEditor | 顶栏 EditorSync 工具条（主服务器 IP + 连接 + 状态）；主体即主服务器 Tab，仅显示 赛事管理与报名 / 成绩与排名 / 文档编辑/输出/打印 |
| RegistrationTool | 顶栏服务器 IP + 连接 + 状态灯 + 修改密码；主体 2 Tab（个人项目报名 / 接力队报名） |
| RemoteDisplayControl | 顶栏服务器 IP + 连接 + 修改密码；主体九宫格按钮（比赛视图 / 出发表 / 组成绩 / 总排名 / 团体积分 / 颁奖典礼 / 纪录榜 / 欢迎画面 / 暂停） |

### 2.5 打开 HTML 客户端

浏览器打开如下页面之一。**首次启动会显示全屏连接遮罩**，输入主服务器 IP 并点击"连接"按钮才能解锁主界面：

- 计时控制台 `race_control.html`
- 大屏显示 `display.html`
- 排名屏 `leaderboard.html`
- 显示控制 `control.html`
- 在线报名 `register.html`
- 检录台 `checkin.html`
- 自动化测试机器人 `test_bot.html`

> 所有客户端的连接遮罩样式与逻辑统一：连接成功隐藏遮罩；断开自动重连；3 次失败弹一次 confirm 允许重新输入 IP；地址持久化在 `localStorage.swimServerHost`。

### 2.6 防火墙

请允许 `SwimmingScoreboard.exe` 通过 Windows 防火墙（专用 / 公用都勾选），否则其它客户端将无法连接 3002 端口。

---

## 3. 系统架构与通讯

```
            ┌──────────────────────────────┐
            │      计时硬件控制器           │
            │   (TCP / UDP / 串口)         │
            └──────────┬───────────────────┘
                       │ 12 字节帧 F1 53 [CMD] D3..D10 F4
                       │（双向：参数 / 设备 / 比赛距离 / 电池 / 校时）
        ┌──────────────▼──────────────────┐
        │   主服务器 SwimmingScoreboard    │
        │   (WebSocket :3002 / JSON 持久化) │
        └──────────────┬──────────────────┘
                       │
   ┌──────────┬────────┼──────────┬──────────────┬──────────┐
   ▼          ▼        ▼          ▼              ▼          ▼
 EXE 远程台  HTML计时  大屏/排名屏  网页/桌面报名  网页/桌面显控  编排端
 (Timing)   (race)   (display)   (register)    (control)    (Editor)
```

### 3.1 三个计时控制台等价

主服务器 / `RemoteTimingControl.exe` / `race_control.html` **功能完全等价**——任何一端按下准备就绪 / M.发令 / 确认本组成绩 / 计时复位，都通过 WebSocket 广播到其它两端 + 硬件，状态保持一致。

### 3.2 数据流单向：服务器 → 客户端

- 运动员姓名 / 代表队 / 反应时 / 分段 / 名次 / 纪录 全部由服务器在广播载荷中直接给出。
- 客户端不再二次重排名次、不再前端字符串拼接推算反应时。
- 配置 / 控制类命令则客户端 → 服务器，再由服务器统一下发硬件 + 回广播。

### 3.3 编排端同步通道

`ScheduleEditor.exe` 通过 `EditorSyncClient`（WebSocket，复用 3002 端口）连主服务器：

| 消息 | 方向 | 用途 |
|---|---|---|
| `EDITOR_IDENTITY` | 编排端 → 服务器 | 连接后身份握手，请求当前 Package |
| `EDITOR_PACKAGE` | 服务器 → 编排端 | 推送当前 CompetitionPackage 整包 |
| `EDITOR_PUSH_PACKAGE` | 编排端 → 服务器 | 编排端自动保存后推回，服务器再 broadcast 给其它编排端 |

`_applyingRemoteSync` 标志防止应用远端 Package 时回环。所有同步走 `AutoSaveData` 钩子，确保任何字段变化都能 30 秒内传到所有编排端。

---

## 4. 硬件通讯协议（命令字节表）

### 4.1 帧格式

```
F1 53 [CMD] [D3] [D4] [D5] [D6] [D7] [D8] [D9] [D10] F4
 │  │   │    └───────────── 8 字节数据 ─────────────┘    │
 │  │  命令码                                          帧尾
 SOH 'S'
```

共 12 字节，D0=`0xF1`（SOH），D1=`0x53`（'S'），D2=命令，D11=`0xF4`（EOT）。

### 4.2 完整命令字节表

| 线上字节 | USER case | 命令名 | 方向 | 版本 |
|---|---|---|---|---|
| 0x16 | 0x06 | Touchpad | 硬件→PC | 原协议 |
| 0x17 | 0x07 | PushButton1（盲表 1） | 硬件→PC | 原协议 |
| 0x18 | 0x08 | PushButton2（盲表 2） | 硬件→PC | 原协议 |
| 0x19 | 0x09 | PushButton3（盲表 3） | 硬件→PC | 原协议 |
| 0x1A | 0x0A | StartingBlock（+D10 抢跳符号位） | 硬件→PC | v05.13 |
| 0x1C | 0x0C | StartCommand（M.发令应答） | 硬件→PC | 原协议 |
| 0x1D | 0x0D | TestCommand（设备测试） | PC↔硬件 | 原协议 |
| 0x20 | 0x10 | TimerReset（计时复位） | PC↔硬件 | 原协议 |
| 0x21 | 0x11 | TimerReady（准备就绪） | PC↔硬件 | 原协议 |
| 0x39 | 0x29 | Set_DateTime（校时） | PC→硬件 | v05.13 |
| 0x3A | 0x2A | Set_PoolSingleOrDoubleTP（单 / 两端触板） | **双向** | v05.16(扩展) |
| 0x40 | 0x30 | PoolConfig | 硬件→PC | 原协议 |
| 0x41 | 0x31 | Set_ArmDelay_Time（D5 盲表代替延迟） | PC→硬件 | v05.13(扩展) |
| 0x42 | 0x32 | SetCommand（子码 0x01-0x0A） | 双向 | v05.13(扩展) |
| 0x43 | 0x33 | Set_MatchEvent（D8 接力标志 + 道次位图） | PC→硬件 | v05.13(扩展) |
| 0x44 | 0x34 | Set_PoolConfiguration_Com1（=0x40 别名） | PC→硬件 | v05.16 |
| 0x46 | 0x36 | Set_TPSBMB_State（设备好坏 + 启用位） | PC→硬件 | v05.13(语义) |
| 0x47 | 0x37 | Set_LaneOpenClose（道次开 / 关） | **双向** | v05.16(扩展) |
| 0x4B | 0x3B | BatteryVoltage（大端 mV / 1000） | 硬件→PC | v05.13 |
| 0x4C | 0x3C | Set_ForceAllOpen（设备状态机覆盖） | PC→硬件 | v05.13 |
| **0x60** | **0x50** | **Set_LaneEnableDisable**（单道屏蔽） | PC→硬件 | **v05.17** ★ |
| **0x61** | **0x51** | **Set_LapRemaining**（剩余圈数同步） | PC→硬件 | **v05.17** ★ |
| **0x62** | **0x52** | **Set_LaneOrder**（正 / 逆序） | **双向** | **v05.17** ★ |
| **0x63** | **0x53** | **Set_FinishPosition**（终点位置） | **双向** | **v05.17** ★ |
| **0x64** | **0x54** | **Set_TimingsBundle**（5 项时间打包） | 硬件→PC | **v05.17** ★ |
| **0x65** | **0x55** | **Set_RefreshDisplay**（重画硬件主控） | PC→硬件 | **v05.18** ★ |
| 0x7F | 0x6F | RunningTime（滚动时间 tick） | 硬件→PC | 原协议 |

> ★ 为 v2026.05.20 期新增。USER case = 线上字节 − 0x10（硬件 dispatcher 按偏移分发）。

### 4.3 关键命令详解

#### 0x43 Set_MatchEvent（比赛准备）

- D3 总圈数 / D4 右触板次数 / D5 左触板次数
- D6 道次位图 0–4（**bit=1 即占用 / 开启**）
- D7 道次位图 5–9
- D8 接力标志（1=接力 / 0=个人）
- **lane 索引按"屏幕位置"编码**：逆序模式下 `hwIndex = 9 − lane`
- 在"准备就绪"按下时发送，紧接 30–50 ms 延迟后再发 `0x21 Ready`

#### 0x60 Set_LaneEnableDisable

- D3 泳道号 0–9（必须单道，**不支持 0xFF**）
- D4 1=启用 / 0=屏蔽（同时切换硬件道次按钮显示）

与 0x47 / 0x4C 三命令的语义区分：

| 命令 | 作用范围 | 效果 |
|---|---|---|
| `0x47` | 全部或单道 | 整体启用 / 禁用 TP/SB/MB（**不动**道次按钮显示） |
| `0x60` | **仅单道** | 同时切换 TP/SB/MB 启用 + 道次按钮显示状态 |
| `0x4C` | 全部或单道 | 设备**状态机覆盖**（接受所有信号 vs 按状态过滤） |

#### 0x61 Set_LapRemaining

- D3 泳道号 0–9
- D4 端：0=左 / 1=右
- D5 剩余圈数 0–255

PC 端"剩余圈数 ▲▼" spinner 调整后立即下发，硬件 `laps[D3][D4] = D5` 并重画对应道的圈数显示。

#### 0x62 / 0x63 双向同步

- `0x62 Set_LaneOrder`：D3=0 正序 / 1 逆序
- `0x63 Set_FinishPosition`：D3=0 终点左端 / 1 终点右端

PC 改设置时下发硬件；硬件 net_test 子界面用户改后回主控时上报，PC 自动更新 `_laneCloseSettings` + `ApplyTouchpadInstallModeToLanes()` 刷新各道 NotInstalled + Broadcast() 推全客户端。**双向命令均禁止回发以防回环**。

#### 0x64 Set_TimingsBundle（硬件 → PC）

硬件 net_test 子界面"参数设置"输入完 5 项时间编辑框后退出，把当前值一帧打包上报：

| 字段 | 含义 | 单位 |
|---|---|---|
| D3 | 泳道封闭时间 LaneCloseTime | 秒 |
| D4 | 触板延迟关闭 ResultConfirmCloseDelay | 秒 |
| D5 | 出发台延迟关闭 StartBlockCloseDelay | 秒 |
| D6 | 盲表代替成绩延迟 BlindReplaceDelay | **秒（整数）** |
| D7 | 成绩显示时间 SplitDisplayTime | 秒 |

> ⚠ `BlindReplaceDelay` 单位 v2026.05.18 起统一为"整数秒"；旧命令 0x41 D5 / 0x42 子码 0x09 是 0.1 秒精度，PC 端 SendXxx 时会乘 10 转换。硬件内部 `MBdelay_Time = d5 * 10`。

#### 0x65 Set_RefreshDisplay

PC 参数对话框 OK 后下发，硬件主控按新参数主动重画一屏，但**不丢失**比赛状态（缺道 / 剩余圈数 / TP/SB/MB Open_State 全部保留再恢复）。D3–D10 全 0。

#### 0x4B BatteryVoltage（硬件 → PC）

D3:D4 大端 mV，主控 = `(D3<<8 | D4) / 1000.0` V。

| 区间 | 颜色 |
|---|---|
| ≥ 7.4 V | 绿 |
| 6.8 ~ 7.4 V | 黄 |
| < 6.8 V | 红 |

接收后实时刷新主服务器"系统硬件"栏 + 远程台顶栏。30 秒无更新自动置 "—"。

#### 0x39 Set_DateTime（周期校时）

连接硬件后立即下发一次；之后 `DispatcherTimer _clockSyncTimer` 每 30 分钟刷一次。字段 D3 YearLo / D4 YearHi / D5 Month / D6 Day / D7 Hour / D8 Min / D9 Sec。

### 4.4 帧间延迟

`TimingBridge.DelayBetweenFrames(int ms = 50)` 用于连续下发时的等待。硬件状态机串行处理，相邻帧间隔不足会丢帧：

| 场景 | 延迟 |
|---|---|
| 0x43 之后 → 0x21 Ready | 20 ms |
| 0x40 之后 → 0x42 子码序列（共 ~9 帧）| 50 ms × N |
| 每道设备状态帧（最多 10 道） | 30 ms × N |
| 0x20 复位后 → 任何后续帧 | 50 ms |

按下"M.发令"的总下发时延约 ~800 ms，业务可接受。

### 4.5 远程 EXE 转发（_REPORT 子命令族）

`RemoteTimingControl.exe` 直连硬件时，把硬件→PC 的扩展命令拆为 WebSocket TIMING_CMD 子命令转发给主服务器：

| 硬件帧 | WebSocket TIMING_CMD | data 字段 |
|---|---|---|
| 0x4B BatteryVoltage | `BATTERY_REPORT` | `{ voltage }` |
| 0x64 TimingsBundle | `TIMINGS_BUNDLE_REPORT` | `{ laneCloseTime, ... }` 共 5 项 |
| 0x62 LaneOrder | `LANE_ORDER_REPORT` | `{ order: "forward"|"reverse" }` |
| 0x63 FinishPosition | `FINISH_POS_REPORT` | `{ pos: "left"|"right" }` |
| 0x3A PoolSingleOrDouble | `POOL_SIDE_REPORT` | `{ single: bool }` |
| 0x47 LaneOpenClose | `LANE_OPEN_CLOSE_REPORT` | `{ lane: -1|0..9, open: bool }` |

主服务器侧 `HandleHardwareReportForwarded` 还原为 `TimingData` 后调 `ProcessTimingDataFromHardware` 复用原处理路径。

仅适用于"EXE 直连硬件"模式；主服务器直连硬件时走 `OnTimingData` 原路径。

---

## 5. 比赛参数设置管理

`比赛参数设置管理` Tab 用于配置 5 种基础维度 + **团体计分名次分**，**任意修改都会即时同步到所有 WPF 下拉、网页报名端、大屏和远程台**（通过 `NotifyMetadataChanged`）：

| 维度 | 用途 | 默认值 |
|---|---|---|
| **比赛项目** | 报名 / 赛程的可选项目（50/100/200米…） | 标准游泳项目 17 项 |
| **组别** | 报名时分类（甲/乙/丙/丁/戊/己/青少年/少年/青年/成年/大师/老年） | 6 个标准组 |
| **性别** | 男 / 女 / 混合 | 三个 |
| **赛次** | 预赛 / 半决赛 / 决赛 / 自定义 | 三个 |
| **组数** | 1组 / 2组 / … 的预设值 | 1-8 |

每个维度都有 **编辑 / 导入CSV / 导出CSV / 下载模板** 4 个按钮：

- 编辑器：弹窗 DataGrid，左侧自动序号 1, 2, 3 …，可增/删行；点击 **确认保存** 立即生效
- 导入 / 导出 CSV 与 Excel/WPS 兼容（UTF-8 BOM）

> ⚠ 组别现为人工分类（如奥运/全运的甲乙丙组），与年龄无关；不再像旧版按 MinAge/MaxAge 自动判定。

### 5.1 团体计分 — 名次分设置

Tab 末尾"团体计分 — 名次分"分组提供「**名次分设置**」按钮，弹出可视化对话框（持久化到 `CompetitionPackage.ScoringConfig`，赛事重新加载继续生效）：

| 字段 | 默认 | 说明 |
|---|---|---|
| 取分人数（前 N 名得分） | 8 | 决赛取前 N 名计分；超出则不得分 |
| 破纪录加分（每项） | 0 | 每破 1 项纪录额外加分（预留字段） |
| 个人项目第 1..N 名得分 | 12, 10, 8, 7, 6, 5, 4, 3 | 每名次得分；输入框数量 ≥ max(8, 历史值) |
| 接力项目第 1..N 名得分 | 24, 20, 16, 14, 12, 10, 8, 6 | 接力翻倍 |
| 组别系数（青少年 / 少年 / 大师…） | 0.8 / 0.8 / 0.7 | 最终得分 = 名次分 × 系数 |

按钮：
- **恢复默认** — 回到 FINA 风格默认值（弹确认）
- **取消** — 不写回
- **保存并重算** — 写入 `_scoringConfig`、立即调用 `CalculateTeamScores` 重算 `_teamScores`、`AutoSaveData` 落盘

---

## 6. 赛事管理与报名

`赛事管理与报名` Tab 顶部为赛事基本信息卡片：

- **赛事名称 / 比赛模式 / 泳池长度 / 泳道数量**（10 道默认 0-9，8 道默认 1-8）
- **开始/结束日期 / 比赛地点**
- **主办方 / 承办方 / 技术代表 / 总裁判长 / 发令员 / 编排长**
- 操作按钮：**保存赛事信息 / 加载赛事 / 新建赛事**

下方"赛事概览统计"实时显示各项目分性别 / 组别的报名人数。

> 💡 删除赛事 / 清除当前库 / 新建赛事时，所有依赖数据的下拉、统计、面板、网页端都会自动同步刷新归零。

---

## 7. 运动员管理

### 7.1 工具栏布局

第 1 行：**添加 / 修改选中 / 删除选中** ｜ **导入CSV / 导出CSV / 导出报名模板** ｜ **号码区间设置**
第 2 行：项目 / 组别 / 性别 / 姓名（模糊）/ 编号（模糊）/ 重置筛选

### 7.2 添加 / 修改 / 删除

- **添加运动员**：弹出对话框，可一次性填齐：姓名 / 性别 / 出生日期 / 身份证 / 代表队 / 单位简称 / 组别 / 联系电话 / 协会注册号 / 备注 / 报名项目 / 报名成绩 等
- **修改选中** / **删除选中**：支持多选（Ctrl/Shift），按 Delete 键也可删除

### 7.3 单位简称

新增列 `单位简称`（CountryShort），用于大屏 / 成绩册简显（如 国队队伍名"广东省体育代表团"显示为"粤"）。

### 7.4 接力队员条目

接力队报名后会自动生成 4 位 **接力队员个人条目**（Notes 以 `接力队员` 开头），它们：
- 在"运动员管理"中**自动隐藏**，避免界面混乱
- 在"接力队管理"中按队伍归类显示
- 在"导出 CSV"时**包含**进去（便于导出全量名单）

### 7.5 号码区间设置

按代表队设置号码段（如 北京队 001-050，江苏队 051-100）。新注册运动员自动在该队号码段内取号；也可手动指定。

### 7.6 CSV 表头格式

`号码 , 姓名 , 性别 , 代表队 , 项目 , 报名成绩 , 年龄 , 出生日期 , 身份证号 , 电话 , 备注 , 组别 , 单位简称`

- **导出 CSV**：当前所有运动员（含接力代表与接力队员）
- **导出报名模板**：空白模板 + 表头 + 2 条示例行；用 Excel/WPS 编辑后用"导入CSV"回灌
- **导入 CSV**：号码列留空时由系统按号码段自动分配

---

## 8. 接力队管理

`接力队管理` Tab：按"性别+项目"分组展示所有接力队，每队列出 4 棒队员。

### 8.1 添加 / 删除接力队

- **添加接力队**：选项目 / 性别 / 队名 / 报名成绩 / 4 棒队员姓名（可留空，后续补）
- **删除选中队伍**：同时清除 `_swimmers` 中的代表条目和 4 位队员条目

### 8.2 防重复登记

接力队以 (队名 + 性别 + 项目) 为唯一键。重复提交同一队伍时**不会新增重复条目**，而是用最新数据覆盖现有队伍（更新报名成绩与棒次）。

### 8.3 同步分组信息

按下"**同步分组信息**"按钮，把代表条目的 Heat/Lane 信息同步到 4 棒队员。这样接力队员个人条目（用于检录核对身份）也带组别 / 道次。

---

## 9. 赛程与分组

### 9.1 一键生成日程

点击 **一键生成日程**，弹窗让用户为所有项目选择赛次方案：

- 只决赛（推荐，适合中小型赛事）
- 预赛 → 决赛
- 预赛 → 半决赛 → 决赛

系统按"组别 → 性别 → 项目 → 赛次"维度合成场次，按距离 / 项目类型自动排序到上午（预赛）/ 下午（半决赛）/ 晚上（决赛）三段：

| 时段 | 起始时间 |
|---|---|
| 上午 | 09:00 |
| 下午 | 14:00 |
| 晚上 | 17:30 |

### 9.2 预赛自动分组（FINA SW 3 规则）

按 **World Aquatics SW 3.1.1.4 / 3.1.1.5 / 3.1.2** 标准实现：

#### 短距离项目（50 / 100 / 200 米）

- `seedHeats = min(总组数, 3)`：**最后 3 组**进行循环（种子）排位
- 前面组进行直接排位，由慢到快编号；最慢组（第 1 组）拿剩余不满人数
- 种子分配：第 1 名 → 最后组中道，第 2 名 → 倒数第 2 组中道，第 3 名 → 倒数第 3 组中道；第 4 名 → 最后组次中道（lanePriority[1]），依次循环填满

#### 中长距离 / 接力（400 / 800 / 1500 米 / 4×N 接力）

- `seedHeats = min(总组数, 2)`：**最后 2 组**进行循环（种子）排位
- 前面组直接排位

> 实现位置：`HeatScheduler.AssignFinaSeeding(...)`，按 `IsShortDistanceEvent(eventName)` 自动判定距离类别。

#### 矛头泳道（Spearhead）分配

| 泳池 | 优先级（最快→最慢） |
|---|---|
| 8 道 | `4, 5, 3, 6, 2, 7, 1, 8` |
| 10 道 | `4, 5, 3, 6, 2, 7, 1, 8, 0, 9` |

成绩最快者必占 4 道；最慢者排到边道（10 道场景下 0/9 道）。

### 9.3 出场编排微调

`出场编排微调` Tab 的 DataGrid 可对每位运动员的"组次/道次"做精细调整。支持：
- **手动均分**：输入"每组 N 人"自动分组
- **同组互换泳道**
- **追加分组**：把新报名的运动员塞到最后一组空位 + 必要时新建组
- **Excel 工作簿导入 / 导出**（取代旧的 CSV，可保留 8/10 道真实泳道编号 0-9）

### 9.4 晋级处理

`成绩与排名` Tab 的 **晋级处理** 按钮 → 弹出 `PromotionQueryWindow`：

- 查询当前赛次的总排名（不分组统一排名）
- 设置晋级人数（默认：预赛→半决赛取 16，半决赛→决赛取 8）
- 一键执行晋级 + 自动分组到下一赛次

每组确认成绩后系统也会**主动询问**是否进入晋级流程（仅当全部组完赛）。

---

## 10. 比赛控制

`比赛控制` Tab 三栏布局：

```
左：赛程导航 + 计时源对比      中：泳道实时状态        右：比赛控制按钮 + 显示控制 + 系统硬件
```

### 10.1 顶部状态栏（v2026.05.19 重做）

主窗口顶部深色横条（`#1E293B`，左中右三栏）：

- **左**：● 绿点 + "竞赛服务运行中"
- **中**：服务器监听地址（如 `0.0.0.0:3002`）
- **右**：
  - ● 红/绿点 + "硬件计时器: " + **"未连接"（红）** 或 **"已连接"（绿）**（粗体）
  - 间隔
  - "控制模式: 本地"

### 10.2 赛程导航（含组别 / 已完赛标记）

树形结构按"单元 → 项目 → 组次"展开：

```
第1单元（2026-05-01上午）
├ [甲组] 男 50米自由泳 预赛
│   ├ 第1组 [已完赛]
│   ├ 第2组 [已完赛]
│   └ 第3组 (共3组)   ← 当前组高亮
├ [乙组] 男 100米自由泳 预赛
...
```

- 项目标签自动加 `[组别]` 前缀
- 组次确认成绩后立即追加 `[已完赛]` 灰色标记
- 当前选中组高亮蓝色

> ✅ **已确认成绩永久锁定**：通过 `_confirmedHeats` 集合记录每个已确认组次（key = 组别|性别|项目|赛次|组次），并入档持久化。

### 10.3 中栏顶部信息块

依次显示：

- 大字"泳道实时状态"
- 当前比赛文本 `[组别] 性别子 项目 赛次 第N/M组`（`PoolCurrentEventText`）
- 当前项目纪录条 `RecordDisplayText`（橙色 18pt：`WR: --- CR: ---`）
- 滚动时间 `RunningTimeText`（32pt 橙，Consolas，格式 `0.00`，硬件 0x7F 推送）
- **比赛状态徽章** `RaceStateBox`：随状态彩色底色 + 白点 + 文字
  - 🔵 等待（蓝 `#3B82F6` 白字）
  - 🟡 准备就绪（黄 `#F59E0B` 黑字）
  - 🔴 比赛中（红 `#EF4444` 白字）
  - ⚫ 已完赛（灰 `#475569` 白字）

### 10.4 泳道实时状态

每条泳道一行，左右对称布局：

```
[T] 盲3 盲2 盲1 出发 触板 [圈 ▲▼] │ 姓名+进度 │ [▲▼ 圈] 触板 出发 盲1 盲2 盲3 [T]
```

- **左右盲表数量**：可独立设置 1-3，超出数量的圆点占位但隐藏
- **T 按钮**：手动触板，左右各一，可独立启用 / 禁用
- **进度条**：箭头方向跟随发令位置；显示运动员所在段
- **接力项目**：第 1 行显示代表队（如"江苏"），第 2 行显示 `第N棒: 队员姓名`（按当前圈数自动推断）
- **空泳道**：整行淡化（透明度 35%），名字显示"（空泳道）"
- **圈数 ▲▼ 微调**：每道左 / 右各一对 spinner。按 ▼ 加圈即时进入下一棒，按 ▲ 减圈回退一棒；值钳到 `[0, 该侧实际触板次数]`，三端（主控 / EXE / HTML）操作一致。
  - PC 同步下发 `0x61 Set_LapRemaining` 到硬件
- **反应时颜色**：硬件检测到反应时低于阈值时变红，行边框琥珀色提示"可疑起跳"。**不再自动 DSQ**——是否判罚由裁判手动按 DSQ。
- **道次方向**：参数设置→道次顺序"正序 0→9"或"逆序 9→0"，三端 UI 同步按选定顺序排列，并通过 `0x62` 下发硬件。
- **设备点五态**：见 §1 本期变更总览。

### 10.5 比赛控制按钮组（v2026.05.19 名称统一）

| 按钮 | 颜色 | 用途 |
|---|---|---|
| **准备就绪** | 黄 `#F59E0B` | 锁定泳道、出发台打开；下发 `0x43 SetMatchEvent` + `0x21 Ready` |
| **M.发令** | 绿 `#22C55E` | 启动计时器、关闭出发台；下发 `0x1C Start` |
| **计时复位** | 红 `#EF4444` | 见 §10.7 两种语义 |
| **确认本组成绩** | 蓝 `#3B82F6` | 锁定本组成绩并永久入档 |
| **打印成绩** | 青绿 `#059669` | 当前组成绩单弹窗 |

> "准备就绪"和"计时复位"默认会弹**单行确认对话框**（v2026.04 防误按）。

### 10.6 泳道操作（2×5 网格按钮）

输入框"泳道操作"+ 输入框（默认 4）+ 后缀"道"。

| 按钮 | 颜色 | 用途 |
|---|---|---|
| DNS | `#64748B` | 选定道弃权 |
| DNF | `#78716C` | 选定道中途退出 |
| DSQ | `#9CA3AF` | 选定道犯规取消成绩 |
| 手动输入 | `#D97706` | 手动录入成绩 |
| 取消备注 | `#0EA5E9` | 清除 DSQ/DNS/DNF |
| 盲表成绩 | `#8B5CF6` | 切到该道盲表成绩 |
| **道次打开** | `#10B981` | 该道整体启用（`0x60` D4=1，硬件同时切按钮显示） |
| **道次关闭** | `#94A3B8` | 该道整体屏蔽（`0x60` D4=0） |
| 全部打开 | `#22C55E` | 所有 10 道整体启用（`0x47` D3=0xFF D4=1） |
| 全部关闭 | `#64748B` | 所有 10 道整体禁用（`0x47` D3=0xFF D4=0） |

### 10.7 显示控制（2×6 网格按钮）

| 按钮 | 颜色 | 推送显示模式 |
|---|---|---|
| 出发表 | `#3B82F6` | `SHOW_START_LIST` |
| 比赛视图 | `#2563EB` | `SHOW_LIVE_RACE` |
| 组成绩 | `#059669` | `SHOW_HEAT_RESULT` |
| 总排名 | `#10B981` | `SHOW_EVENT_RANKING` |
| 团体 | `#D97706` | `SHOW_TEAM_STANDINGS` |
| 颁奖 | `#F59E0B` | `SHOW_AWARDS` |
| 纪录 | `#DC2626` | `SHOW_RECORDS` |
| 欢迎 | `#8B5CF6` | `SHOW_WELCOME` |
| **成绩发布**（整行） | `#F59E0B` | 选已完赛组次锁定推送 |
| **图片 / 视频**（整行） | `#0EA5E9` | 上传媒体全屏播放 |

### 10.8 系统硬件 (v2026.05.14+)

| 控件 | 颜色 | 用途 |
|---|---|---|
| **网络连接** | 绿 `#22C55E` | 用 `timing_connection.json` 中 TCP 主机/端口快速连接（沿用 `QuickConnectSerial` 控件名，按钮文字 v2026.05.14 改为"网络连接"，Click 走 TCP） |
| **设备测试** | 青 `#0EA5E9` | 切 `_testMode`；进入时清空 buffer + 所有非损坏设备 Open + 下发 `0x1D`；硬件来什么数据都接收显示 |
| **电池电压** | 自动绿/黄/红 | `0x4B BatteryVoltage` 上报后实时显示；30 s 无更新置 "—" |

详细操作流程：

#### 设备测试模式（Device Test）

1. 点"设备测试" → 进入 `_testMode = true`
2. 清空 `_rawTimingLog` / `_testLastEventLeft` / `_testLastEventRight`、滚动时间归零
3. 所有非 Broken / 非 NotInstalled 设备置 Open
4. 下发 `0x1D Test_Command` 给硬件
5. 硬件来事件 → 跳过比赛状态 / 空泳道过滤 → 写日志 `【测试】泳道X 类型 时间`
6. 该端设备点 **红闪 500 ms 后回绿**；姓名栏显示左端事件文字（"触 23.45" / "出发 0.42"），成绩栏显示右端事件文字
7. 三端（服务器+HTML+EXE）状态徽章覆盖红色"设备测试"
8. 测试期间不创建 split / 不计成绩 / 不进 Touched 窗口
9. 再点一次按钮退出，所有设备 Closed

HTML / EXE 通过 WebSocket `DEVICE_TEST_TOGGLE` 命令请求，服务器统一切换。

### 10.9 计时复位的两种语义

按"当前组是否已确认成绩"自动选择行为：

#### A) 已确认成绩的组（赛程导航已显示 [已完赛]）

- **彻底清零复位**，准备进入下一组比赛
- **已确认成绩永久保留**（在 `_confirmedHeats` + 各运动员 `Results` 中已落盘）
- 自动 `SetCurrentHeat(N+1)` 切到下一组
- 若已是本赛次最后一组：泳道行整体清空（"（空泳道）"），顶部"组别"行改为"第 N 组已完赛 / 共 M 组"，**大屏切到欢迎画面**

#### B) 未确认成绩的组（如抢跳召回重赛）

- 只清当前组的时间 / 分段数据
- **DSQ / DNS / DNF 等备注状态保留**
- 停留在当前组，重新发令

### 10.10 切组守卫

当前组未确认成绩时，禁止切换到其它项目 / 组次（保护操作员避免误操作丢成绩）。三个计时控制台（主控 / EXE / HTML）规则一致。

### 10.11 已确认组防再次开赛

"准备就绪"按钮：当前组已确认时，弹"操作被阻止"提示，必须切到下一组才能继续。

### 10.12 起跳犯规处理（人工判罚 + DSQ 道继续比赛）

| 步骤 | 系统行为 |
|---|---|
| 硬件检测到反应时 < `抢跳判定阈值` | 仅置 `IsSuspectFalseStart=true`，反应时数字变红、行边框 amber 提示 |
| 三端日志 | 输出 `⚠ 起跳可疑（待裁判确认）泳道X 反应时:0.082s` |
| 裁判判罚 | 在该泳道按 **DSQ** 按钮；备注栏立即显示 `DSQ`、运动员状态置 DSQ |
| 裁判不判罚 | 反应时仍标红，但成绩照常计入；后续可继续比赛 |
| **DSQ 道继续接收原始数据** | 标 DSQ 后泳道仍处于"接收态"——触板/盲表/分段/反应时全部继续记录并落盘；倒计时结束后下一段触板/盲表/出发台照常打开。仅大屏排名 / 最终成绩排除该道 |

> 实现：`IsLaneReceivingData(lane)` 用于"原始数据接收 + 设备开关"判定（DSQ 通过），`IsLaneParticipating(lane)` 仍用于"排名 / 积分"判定（DSQ 排除）。两者职责分离。

### 10.13 接力 4 棒反应时

每棒交接时，服务器按棒次索引覆盖写入 `LaneResult.LegReactionTimes[legIdx]`：

- **第 1 棒反应时** = 出发台时间（相对发令信号）
- **第 N+1 棒反应时** = 第 N+1 棒出发台时间 − 第 N 棒触板累计时间（N=1, 2, 3）
- 由 `ComputeRelayLegIndex(currentLap)` 推算棒次索引，按索引"覆盖写入"
- 同步广播 `sw.reactionSeq`（`LaneDeviceState.ReactionTime` setter 每次自增），客户端用它检测"新一棒反应时"，重启大屏备注栏 5 秒倒计时窗口
- 个人项目仍是 `StartingBlockTime` 单值

### 10.14 备用成绩展示（争议时使用）

进入"查询比赛原始数据"窗口（`Database/RawData` 下 `.txt` / `.html`）每位运动员"分段明细"行下方，**Touched 窗口期内捕获的额外计时事件**作为"备用成绩"列出：

```
★备用成绩(争议时使用):  触板[26.45]  盲表[1:26.51, 2:26.49]  出发台[left:0.085]
```

HTML 版本用红底加粗突出。三种数据都为空时整行不出现。

| 设备 | Touched 保持时间 | 期间事件归属 |
|---|---|---|
| 触板 | `ResultConfirmCloseDelay`（默认 3 s） | 写日志 + 加入 `LaneResult.BackupTouchTimes` |
| 盲表（按单只） | `ResultConfirmCloseDelay` | 写日志 + 加入 `LaneResult.BackupBlindTimes`，格式 `盲号:时间` |
| 出发台 | `StartBlockCloseDelay`（默认 3 s） | 写日志 + 加入 `LaneResult.BackupReactionTimes`，格式 `端:时间` |

---

## 11. 成绩与排名

`成绩与排名` Tab：

### 11.1 筛选器

6 维筛选：组别 / 性别 / 项目 / 赛次 / 组次 / 名次

- **组别下拉**：自动列出 `_ageGroups` 主列表 + 已报运动员中实际出现的组别
- **组次下拉**：只列出**已确认成绩**的组次（未确认的隐藏）

### 11.2 成绩表（DataGrid）

列：**名次 / 道 / 号码 / 姓名 / 代表队 / 最终成绩 / 计时源 / 反应时间 / 状态 / 纪录**

- **接力赛姓名 / 代表队**：`姓名` 列直接显示 4 名队员（逗号分隔），`代表队` 列显示队名。
- **姓名列**：220 px，`DataGridTemplateColumn` + `TextWrapping=Wrap`，过长时自动换行。
- **反应时间列**：接力赛展开为 `第1棒:0.45  第2棒:0.30  第3棒:0.51  第4棒:0.22`，170 px 启用换行；个人项目仍单值。未记录到的棒次显示 `—`。

只显示**已确认成绩**的组（`IsHeatConfirmed` 真）。

### 11.3 并列名次

`UpdateHeatRanking` / `ComputeLiveRanks` / `GetEventRanking` 三处排名计算统一使用 **competition ranking 1-2-2-4** 规则：

例：A=54.50（1）、B=54.68（2）、C=54.68（2）、D=54.99（4）。

打印的每组成绩表中，并列名次以 `=2` 形式标注。

> **判定精度**：硬件计时控制器以 1/100 秒精度（单字节）发送成绩。软件侧把成绩按 `Math.Round(t × 100, MidpointRounding.AwayFromZero)` 取长整数（百分秒）后做相等比较：两条都 round 到同一个百分秒整数即视为并列。5 处排名计算 + `EnforceOnlyLeaderRecordNote` 全部走同一辅助 `IsTieTime`。

### 11.4 团体计分

- 决赛取前 N 名计分（N 由「比赛参数设置管理 → 名次分设置」配置，默认 8）
- 个人 / 接力 各名次得分、组别系数（青少年=0.8、大师=0.7 等）、破纪录加分均可视化编辑
- 并列名次的运动员均按其名次取分；金 / 银 / 铜也并列发放
- "成绩与排名" Tab 工具栏的 **「团体计分」** 按钮：根据当前配置重新计算 `_teamScores` 并弹出 `TeamScoreWindow`

### 11.5 查询原始数据

弹窗选择已保存的"项目原始数据"文件（`Database/RawData/<组别_>性别_项目_赛次_第N组.txt + .html`），查看每组细节：

- **出场名单**：拆「代表队」+「运动员姓名」两列；接力赛 4 名队员姓名从 `Notes` 提取
- **原始数据流**：时刻 / 泳道 / 类型 / 时间 / 运动员，按时间序排列
- **最终成绩**：拆「代表队」+「运动员姓名」两列；反应时按棒展开（txt 用 `第1棒:0.45 / ...`，HTML 用 `<br>` 堆叠每棒一行）；触板 / 盲1 / 盲2 / 盲3 各自的最终值同表展示
- **分段明细**：每段触板 / 盲1 / 盲2 / 盲3 / 手动 / 累计 / 计时源裁定明细
- **备用成绩（争议时使用）**：见 §10.14

### 11.6 操作按钮

- **晋级处理**：见 §9.4
- **团体计分**：见 §11.4

---

## 12. 纪录管理

### 12.1 数据维护

- 支持类型：世界纪录 / 奥运会纪录 / 亚洲纪录 / 亚洲青年纪录 / 全国纪录 / 省运会纪录 / 市纪录 / 赛会纪录（可在大屏顶部"主纪录"切换显示）
- 字段：项目 / 性别 / 类型 / 保持者 / 代表队 / 成绩 / 日期 / 地点
- 操作：**添加 / 修改 / 删除 / 导入 CSV / 导出 CSV / 导入预置纪录**
- 大屏顶部显示 **当前项目的"主纪录" + "赛会纪录 CR"** 两条；主纪录类型可在该面板下拉调整

### 12.2 简写标识表

| 纪录类型 | 破纪录标签 | 平纪录标签 |
|---|---|---|
| 世界纪录 | `WR` | `=WR` |
| 奥运会纪录 | `OR` | `=OR` |
| 亚洲纪录 | `AR` | `=AR` |
| 亚洲青年纪录 | `AJR` | `=AJR` |
| 全国纪录 | `NR` | `=NR` |
| 省运会 / 省纪录 | `省R` | `=省R` |
| 市纪录 | `市R` | `=市R` |
| 赛会纪录 | `CR` | `=CR` |

多条破纪录用 `/` 拼接（如 `WR/AR/NR`），按 WR > OR > AR > AJR > NR > 省R > 市R > CR 顺序排序。

### 12.3 自动比对工作流

```
触板完赛  → CheckRecords() → 写 result.RecordNote
                          → EnforceOnlyLeaderRecordNote() → 仅本组第 1 名（含 5 ms 内并列）保留
                                    ↓ 广播 + UI/打印 显示
                                    ↓ 裁判 ConfirmResult
                              UpdateRecordsAfterConfirm() → 刷新 SwimmingRecord
                                    ↓ AutoSaveData
                              入档持久化（CompetitionPackage._records）
```

| 阶段 | 行为 |
|---|---|
| **完赛瞬间** | `CheckRecords` 遍历所有匹配 `项目+性别+组别(可空)` 且 `Time>0` 的记录条目；`FinalTime < record.Time` 写破纪录、`FinalTime == record.Time` 写平纪录；记录库为空则不比较 |
| **本组只保留第 1 名** | `EnforceOnlyLeaderRecordNote` 在 `CheckRecords` 末尾及 `UpdateHeatRanking` 末尾运行：本组多人破同一记录时，仅本组最快者（含并列）保留标识，符合 FINA 惯例 |
| **同步显示** | `result.RecordNote` 通过 `Broadcast` 同时推到主控泳道行、EXE 远程台、`race_control.html`、`display.html`（备注栏金色 `#FBBF24`） |
| **打印输出** | 每组成绩打印模板的成绩单元格后追加 `<span style='color:#b45309;font-weight:bold;'>WR</span>`；"成绩与排名" Tab 的"纪录"列同步填值 |
| **裁判确认** | 按 **确认本组成绩**，`UpdateRecordsAfterConfirm` 把仍 `< record.Time` 的成绩写回 `SwimmingRecord`：刷新 `HolderName / HolderCountry / Time / TimeInSeconds / Date(今天) / Location(_competitionName)`，落盘到 CompetitionPackage |
| **平纪录不刷新** | `=` 前缀（平纪录）只标识，不更新原条目 |
| **DSQ 取消标识** | 裁判把第 1 名标 DSQ 后，`UpdateHeatRanking` 重算时新晋第 1 名自动获得标识，原第 1 名（已 DSQ）的标识被清空 |

### 12.4 破纪录统计表

成绩册中可勾选的"破纪录统计表"自动收集本届被改写的纪录条目：项目 / 类型 / 保持者 / 单位 / 成绩 / 日期。

---

## 13. 文档编辑 / 输出 / 打印

`文档编辑/输出/打印` Tab 提供两本核心文档的可视化编辑 + 三大类即时打印：

### 13.1 秩序册

- 封面（标题 / 副标题 / 场地图）
- 序言 / 致辞
- 主办方 / 承办方
- 技术官员名单 / 裁判员名单
- 项目秩序（按编排自动生成）
- 闭幕词

### 13.2 成绩册

- 封面 / 序言
- 成绩排名（按项目自动生成）
- 团体积分 / 颁奖统计
- 纪录统计
- 全部成绩（含分段、可选）
- 闭幕词

输出格式：**HTML（在线预览） / DOC（Word） / PDF（打印）**

### 13.3 接力 4 棒反应时输出

所有"打印 / 输出"路径均按棒次展开接力反应时；个人项目仍为单值（`StartingBlockTime`）。

| 路径 | 输出格式 | 列宽 |
|---|---|---|
| 本组成绩单 `BuildHeatResultsHtml` | `第1棒:0.45<br>第2棒:0.30<br>...` | 反应时列 70 px |
| 赛事级成绩公告 `BuildEventResultsHtml`（含分段标记） | 同上 | 反应时列 110 px（接力时） |
| 单项成绩单弹窗 `EventResultPrintWindow` | 预览：`第1棒:0.45  第2棒:0.30 ...`（双空格 + TextWrapping=Wrap）<br>打印：`<br>` 强制每棒一行 | 窗口 1100×560 px；姓名 220 px、反应时 170 px |
| 原始数据保存（txt + html） | txt：`第1棒:0.45 / 第2棒:0.30 / ...`<br>html：`<br>` 堆叠 | — |
| ResultGrid（成绩与排名 Tab） | 双空格分隔，列 170 px + TextWrapping=Wrap | — |

未记录到的棒次显示 `—`。所有路径都从 `LaneResult.LegReactionTimes[]` 读取（同一份数据源）。

### 13.4 分段计时报告组次选择器

按"打印分段计时报告"按钮 → 弹出"已完赛组次树"（结构同赛程导航）：

```
第1单元
└ [甲组] 男 50米自由泳 预赛
    ├ 第1组 [已完赛]
    └ 第2组 [已完赛]
```

仅显示按下"确认本组成绩"的组；选中后才打印；按"取消"则不打印。

### 13.5 分段计时报告合并表

报告改为"一组一张大表"，便于跨道横向比较分段：

| 道 | 姓名 | 代表队 | 50m | 100m | 150m | 200m | 最终成绩 |
|---|---|---|---|---|---|---|---|
| 4 | 张三 | 上海 | **27.50**<br>(27.50) | **57.20**<br>(29.70) | **1:27.30**<br>(30.10) | **1:58.45**<br>(31.15) | **1:58.45** |
| 5 | 李四 | 北京 | **28.00**<br>(28.00) | **58.10**<br>(30.10) | **1:28.50**<br>(30.40) | **1:59.20**<br>(30.70) | **1:59.20** |

- 每段单元格双行：粗体上行 = 累计时间，灰色小字下行（带括号）= 本段时间
- 距离列自动取本组所有运动员分段距离的并集，按升序排
- 缺失分段显示 `—`
- 接力赛：列名换为「代表队 / 队员」

### 13.6 录取标志 Q

预赛 / 半决赛打印的成绩单中，已晋级到下一赛次的运动员在备注栏标 **Q**（绿色加粗）：

| 当前赛次 | 下一赛次（自动判断） |
|---|---|
| 预赛 | 若 schedule 含半决赛 → 半决赛；否则 → 决赛 |
| 半决赛 | 决赛 |
| 决赛 | （无） |

DSQ/DNS/DNF/DQ 比 Q 优先；判罚的运动员不显示 Q。

---

## 14. 显示控制（大屏 / 排名屏 / 媒体）

### 14.1 大屏显示模式

主控的"显示控制"面板可远程切换大屏（`display.html`）显示模式：
- **比赛视图**：泳道行实时成绩
- **出发表 / 组成绩 / 总排名 / 团体 / 颁奖 / 纪录 / 赛程表 / 晋级名单**
- **欢迎画面 / 暂停画面**

桌面版 `RemoteDisplayControl.exe` 提供同样的九宫格按钮（比赛视图 / 出发表 / 组成绩 / 总排名 / 团体积分 / 颁奖典礼 / 纪录榜 / 欢迎画面 / 暂停），适合搭配大屏运维位单独使用。

### 14.2 大屏比赛视图布局

每条泳道一行，6 列居中显示：

| 列 | 内容 | 宽度（直播视图） |
|---|---|---|
| 道次 | 1, 2, 3 ... | 5vw |
| 姓名 | 个人：姓名；接力：当前棒"第N棒: 队员姓名"动态显示 | 14vw |
| 代表队 | `countryShort || country` | 12vw |
| 成绩 | 完赛后显示 finalTime；比赛中显示最近一次真实分段（5 秒窗口） | 14vw |
| 名次 | 服务器广播的 `sw.rank`（直接渲染） | 5vw |
| 备注 | DSQ/DNS/DNF/DQ > 破纪录（金）> 反应时（5 秒窗口） | 8vw |

**接力成绩榜视图**：姓名列 36 vw、代表队 8 vw，4 名队员逗号分隔展示。

### 14.3 备注栏优先级

```
1) DSQ/DNS/DNF/DQ —— 实时显示（不等待"确认本组成绩"），CSS 默认红色 var(--red)
2) 破/平纪录 recordNote —— 金色 #FBBF24
3) 反应时显示窗口 —— splitDisplayTime 倒计时（默认 5 秒）
   • 接力赛每棒交接由 reactionSeq 触发新窗口（每棒都显示一次）
   • 抢跳可疑 / 已判罚 → 反应时标红 #EF4444
   • 正常 → 浅色 #F8FAFC
```

DSQ/破纪录两类不会被反应时覆盖。

### 14.4 大屏分区刷新

- **顶部静态文字**（赛事名 / 项目 / 阶段·组 / 纪录条）：仅在数据变化时写入 textContent
- **滚动时间右上角**：100 ms 独立 ticker 本地插值
- **泳道行 DOM**：仅在 `buildKey`（项目 / 组 / 泳道布局 / 接力切换）变化时重建一次，缓存到 `_raceRowDOM[]`
- **泳道字段**（道次 / 姓名 / 队伍 / 成绩 / 名次 / 备注）：250 ms 廉价刷新 + 收到广播时刷新；字段对比，仅写差异
- **滚动时间在确认成绩后清空**：`_updateRunningTimeOnly` 检测 `data.resultConfirmed === true` 时直接清空文本

### 14.5 空泳道不显示

`_activeSwimmersForDisplay` 滤除 `status==='EMPTY'` 或无姓名+无号码的占位行；`isOccupiedLane` 过滤 0 道无人。

### 14.6 隐私保护

服务器广播载荷取消 `notes` 字段，避免运动员个人备注（姓名 / 单位 / 教练等）泄露到大屏；报名端通过独立的 `REGISTER_QUERY_RESULT` 通道获取 notes。

### 14.7 图片 / 视频全屏

支持 PNG/JPG/MP4 全屏推送到大屏，三种贴合方式（contain / cover / fill）。视频可循环、静音、自动播放。

---

## 15. 系统日志与数据管理

`系统日志与数据` Tab：

- **日志窗口**：实时输出操作 / 状态 / 错误日志，便于排查偶发问题
- **数据备份**：JSON 自动保存（每次确认成绩 / 关键操作）+ 手动导出
- **历史赛事**：按"赛事名称"列出可加载的存档文件
- **删除当前赛事**：清空所有数据 + 同步刷新所有面板

---

## 16. 参数设置与硬件连接

主控的"系统工作状态" Tab 内有 **参数设置** / **硬件计时器连接** 二级 Tab；"比赛控制"右栏点 **"⚙ 参数设置"** 按钮也弹同一对话框。

### 16.1 参数设置对话框（420 px 宽，自适应高度，深色背景 `#1E293B`）

| 参数 | 默认 | 单位 / 类型 | 说明 |
|---|---|---|---|
| 泳道关闭时间 | 20 | 秒 | 触板触碰后多久关闭其它设备 |
| 出发台关闭延迟 | 3 | 秒 | 发令后延迟多少秒关出发台 |
| 成绩确认关闭延迟 | 3 | 秒 | 确认成绩后延迟关闭出发台 |
| 抢跳判定阈值 | 0.10 | 秒 | 反应时低于此值视为"可疑抢跳"（仅标红，不自动 DSQ） |
| 分段成绩显示时长 | 5 | 秒 | 大屏显示分段时间 — 也是反应时显示窗口时长 |
| **盲表代替成绩延迟** | 5 | **整数秒（0–9）** | v2026.05.18 单位统一 |
| 第 1 名成绩停留时间 | 3 | 秒 | 右上角"第 1 名分段成绩"绿色提示秒数 |
| 大屏翻屏时间 | 5 | 秒 | 总排名等多页内容自动翻页周期，1–60 秒 |
| **终点位置** | 左端 | 单选 | 左端 / 右端，变化时联动刷新各道 NotInstalled |
| **反应时(RT)** | 打开 | 单选 | 打开 / 关闭 出发反应时检测 |
| **道次顺序** | 正序 0→9 | 单选 | 正序 / 逆序，下发硬件 `0x62` |
| **泳池触板** | 两端安装 | 单选 | 两端 / 单边，下发硬件 `0x3A`；硬件 SD 卡持久化 |

子按钮（弹出二级对话框）：
- **设备状态管理**（紫 `#8B5CF6`）— 配置每道触板 / 出发台 / 盲表的 损坏 / 未安装 标志
- **手动按键管理**（青 `#0EA5E9`）— 启用 / 禁用每道左右手动 T 按钮
- **左右盲表数量设置**（琥珀 `#F59E0B`）— 每端 1-3 个

#### 16.1.1 反应时(RT) 开 / 关详解

- **打开（默认）**：硬件检测到出发台触发后写入反应时；反应时低于阈值时自动标红"可疑起跳"
- **关闭**：所有反应时相关处理统一跳过——
  - `HandleFalseStartDetected` 直接 return
  - 出发台命中处理（普通+接力交接）：不写 `ReactionTime`、不判定可疑、不再标红
  - `result.StartingBlockTime` 不写入；接力 `LegReactionTimes` 不更新
  - 三端泳道行 `ReactionText` 与计时源详情面板：显示空 / "已关闭"
  - 广播 `reactionTime` 字段为空字符串

#### 16.1.2 进出对话框保存恢复（v2026.05.18-2）

进入对话框时先快照各道 `IsFinished` / `LeftLapManualAdjust` / `RightLapManualAdjust`：

- 按 **取消**：放弃所有改动
- 按 **确定**：应用新参数后**恢复**这三个字段（避免参数微调误清零操作员手调的剩余圈数）；之后下发 `0x65 SetRefreshDisplay` 让硬件按新参数主动重画一屏

#### 16.1.3 终点位置变化联动

切换"终点位置"或"泳池触板"后，自动触发：

1. `ApplyTouchpadInstallModeToLanes()` — 单端模式下"非终点端"自动标 NotInstalled
2. `SendDeviceStatusesToHardware()` — 0x46 / 0x49 / 0x4A 帧反映更新后的状态
3. `Broadcast()` — 推全客户端

### 16.2 硬件计时器连接

- **协议类型**：TCP 客户端 / UDP（局域网）/ 串口
- **TCP**：主机 + 端口
- **UDP**：本地监听端口 + 目标主机+端口
- **串口**：端口号下拉 + 波特率（默认 9600，常用 115200）
- **测试 / 连接 / 断开** 按钮 + 状态灯

连接成功后：
1. 立即下发 `0x39 Set_DateTime` 校时
2. 启动 `_clockSyncTimer`，30 分钟刷一次
3. 显示电池电压（如收到 `0x4B`）
4. 顶部"硬件计时器: 已连接"绿色

配置持久化到 `timing_connection.json`（详见附录 B）。

---

## 17. RemoteTimingControl EXE

`RemoteTimingControl.exe`：独立 PC 上的客户端，与主服务器功能等价。

### 17.1 登录

进入 Suite 风格深色登录窗：用户名 / 密码 / 记住登录名和密码 / 登 录。首次 admin/admin。

### 17.2 顶栏（v2026.05.19 重做）

```
[当前比赛信息]  [比赛状态徽章]  ● 硬件 4.23V  服务器:[IP:端口] [连接]  [连接串口][设备测试][设备全开]  [⚙参数设置]  [修改用户名和密码]
```

- 左：当前组别 / 项目 / 赛次
- 比赛状态徽章：等待 / 准备就绪 / 比赛中 / 已完赛（四色）
- **硬件状态指示**：红 ● = 未连接 / 绿 ● = 已连接，旁边显示当前电池电压
- 服务器 IP 输入 + 连接 / 断开按钮
- **系统硬件 3 按钮**：连接串口（绿） / 设备测试（青） / 设备全开（灰）
- **⚙ 参数设置**（灰）
- 修改用户名和密码

### 17.3 主体三列

| 列 | 内容 |
|---|---|
| 左 220 px | 赛程导航 TreeView + 计时源对比（分段下拉 + 信息文本） |
| 中 弹性 | WR/CR + 滚动时间 + 表头 + 泳道行（10 道 × 56 px） + 日志区 |
| 右 200 px | 比赛控制（准备就绪 / M.发令 / 计时复位 / 确认本组成绩） + 泳道操作（2×5）+ 显示控制（2×4） |

### 17.4 比赛控制按钮（同主服务器）

| 按钮 | 颜色 |
|---|---|
| 准备就绪 | 黄 `#F59E0B` |
| M.发令 | 绿 `#22C55E` |
| 计时复位 | 红 `#EF4444` |
| 确认本组成绩 | 蓝 `#3B82F6` |

### 17.5 泳道操作（2×5）

DNS / DNF / DSQ / 手动输入 / 取消备注 / 盲表成绩 / 道次打开 / 道次关闭 / 全部打开 / 全部关闭。

### 17.6 显示控制（2×4 + 成绩发布）

出发表 / 比赛视图 / 组成绩 / 总排名 / 团体 / 颁奖 / 纪录 / 欢迎 / 成绩发布。

### 17.7 守卫机制（与主控一致）

- 已确认成绩的组：禁止再次"准备就绪"
- 当前组未确认成绩：禁止切换组次（弹"操作被阻止"）
- 比赛中：禁止切换项目

### 17.8 参数设置对话框

弹窗 420 px 宽，含与主服务器相同的全部字段（含**道次顺序** 单选 + **泳池触板** 单选）。`applySettings` 用 `SET_LANE_CLOSE_SETTINGS` 上报。

**硬件连接面板**：直接配置 EXE 自身的硬件直连（不经主服务器代理）——串口 / UDP / TCP 三种，连接成功后 EXE 同时与主服务器和硬件双向通讯，硬件→PC 帧通过 §4.5 的 `_REPORT` 子命令族转发给主服务器。

### 17.9 快捷键

- F5 = 准备就绪
- F6 = M.发令
- F7 = 计时复位
- Enter = 确认本组成绩
- Ctrl+← / Ctrl+→ = 上一组 / 下一组
- 数字键 0-9 = 选中泳道
- D / F / Q = 当前选中泳道标记 DNS / DNF / DSQ

---

## 18. HTML 浏览器计时控制台

`race_control.html`：浏览器版的等价控制台，所有按钮 + 守卫规则与 EXE 完全一致。

启动时显示**全屏连接遮罩**，输入服务器 IP → 点击"连接" → 连接成功后遮罩消隐进入主界面。连接失败保留遮罩 + 状态提示，自动重连。

### 18.1 顶 nav（v2026.05.14+ 移自参数设置对话框）

```
[当前组别 项目 赛次]  [比赛状态徽章]  ● 硬件:已连接 4.23V  [连接串口] [设备测试] [设备全开]  [⚙ 参数设置]
```

### 18.2 泳道行布局

```
[发令绿条] [道次] [姓名+队伍] [方向箭头] [赛道] [关闭倒计时] [反应时] [最终成绩] [名次] ● 设备点 [T手动]
```

### 18.3 圈数 spinner / 反应时标红 / 备注栏

每条泳道的左 / 右 "圈" 数字两侧均带 ▲▼ spinner（按哪边只调那边，钳到该侧实际触板次数）。点击发 `ADJUST_LAP_DISPLAY` 命令到服务器。

反应时 `lr-reaction` 单元在 `isSuspectFalseStart || isFalseStart` 时变红 `#EF4444`；备注 `lr-remark` 在非 DSQ 时显示破 / 平纪录标识，金色 `#F59E0B`。

### 18.4 参数设置（与主服务器同步）

弹窗包含全部参数。**v2026.05.14 起**"网络连接 / 设备测试 / 设备全开"按钮已移到顶 nav，对话框只保留参数字段 + 硬件连接 + 服务器地址。`localStorage.swimReactionTimeEnabled` / `swimBigDisplayPageInterval` 持久化，下次启动即使尚未连服务器也用上次值。

---

## 19. ScheduleEditor 编排记录及成绩处理 EXE

`ScheduleEditor.exe`：套件中新增的"轻量化"主程序，专供编排员 / 成绩员 / 文档员协同工作。

### 19.1 部署模型

- 项目文件 `ScheduleEditor.csproj` 复用 `SwimmingScoreboard\MainWindow.xaml` + 所有支撑窗口 / 数据类（通过 `<Page Link>` / `<Compile Link>`）
- 独占文件：`App.xaml` / `LoginWindow.xaml` / `CredentialStore.cs` / `ChangePasswordWindow.cs`
- 运行时 `SwimmingScoreboard.MainWindow` 检测入口程序集名为 `ScheduleEditor` 后自动切到"编排记录及成绩处理"模式

### 19.2 自动应用的差异

| 项目 | 主服务器模式 | 编排器模式 |
|---|---|---|
| 窗口标题 | 游泳赛事管理主服务器 | 游泳赛事管理系统 — 编排记录及成绩处理 |
| 显示 Tab | 全部 7 个 | 3 个：赛事管理与报名 / 成绩与排名 / 文档编辑/输出/打印 |
| WebSocket 服务器 :3002 | 启用 | 跳过 |
| 计时硬件桥接 | 启用 | 跳过 |
| 串口端口下拉 | 填充 | 跳过 |
| 顶栏右侧 | "控制模式: 本地" | EditorSync 工具条：主服务器: [IP] [连接] [状态] |
| 启动日志 | "系统启动完成" | "编排记录及成绩处理 启动完成" |

### 19.3 主服务器协同（EditorSyncClient）

- 通过 WebSocket 连主服务器 3002
- 三种消息：`EDITOR_IDENTITY`（握手）/ `EDITOR_PACKAGE`（服务器→编排端整包）/ `EDITOR_PUSH_PACKAGE`（编排端→服务器整包）
- 主服务器 `AutoSaveData` 后通过 `PropagateSyncAfterSave` 推 `EDITOR_PACKAGE` 给所有连接的编排端
- 编排端 `AutoSaveData` 后发 `EDITOR_PUSH_PACKAGE` 回主服务器；主服务器 `HandleEditorPushPackage` 应用后再 broadcast 给其它编排端
- `_applyingRemoteSync` 标志防应用远端 Package 时回环

### 19.4 适用场景

- 比赛日多个编排员同时录成绩 / 修改晋级名单 / 编辑成绩册
- 主控位（连硬件）和编排位（不连硬件、不开端口）解耦运行
- 主控 PC 卡顿时编排员仍可独立工作

---

## 20. RegistrationTool / RemoteDisplayControl

### 20.1 RegistrationTool.exe（桌面注册工具）

桌面端运动员注册客户端，登录后主界面：

- 顶部：服务器 IP + 连接 + 状态灯 + 修改用户名和密码
- 2 Tab：
  - **个人项目报名**：姓名 / 性别 / 出生日期 / 身份证号 / 代表队 / 组别 / 联系电话 / CSA号 / 备注 + 项目列表 + "+ 添加参赛项目" / "X 删除此项" / "提交全部报名信息"
  - **接力队报名**：队名 / 性别 / 组别 / 单位简称 / 项目 / 报名成绩 + 4 棒队员（姓名 / 出生日期 / 身份证号 / 参赛号）+ "提交接力报名"
- 接力 DatePicker `Language="en-CA" SelectedDateFormat="Short"` 强制 `yyyy-MM-dd`

### 20.2 RemoteDisplayControl.exe（桌面显控）

九宫格按钮，等价于网页 `control.html`：

| 行 | 列 1 | 列 2 | 列 3 |
|---|---|---|---|
| 1 | 比赛视图（绿） | 出发表（蓝） | 组成绩（紫） |
| 2 | 总排名（琥珀） | 团体积分（青） | 颁奖典礼（粉） |
| 3 | 纪录榜（红） | 欢迎画面（slate） | 暂停（深 slate） |

适合搭配大屏运维位单独使用。

---

## 21. 网页检录台

`checkin.html`：检录员核对身份、登记 DSQ/DNS/DNF。

- 左侧：单元 / 项目 / 组次树
- 右侧：选定组的 8/10 道运动员表
  - 列：泳道 / 号码布 / 姓名 / 身份证号 / 单位 / 状态备注（下拉）
  - 接力队员前缀 `<span class="leg-tag">第N棒</span>`
  - 状态下拉：正常 / DNS 弃权未到 / DNF 中途退出 / DSQ 犯规——选中后同步到比赛控制端 + 大屏

启动连接遮罩，与 `display.html` / `race_control.html` 风格一致；持久化 key `localStorage.swimServerHost` 共享。

---

## 22. 大屏显示 / 排名屏

### 22.1 `display.html` 比赛实时大屏

显示模式：见 §14.1。启动时显示连接遮罩（输入主服务器 IP 后才解锁主界面）。

### 22.2 `leaderboard.html` 排名屏

启动时显示连接遮罩 + IP 输入框。`requestAnimationFrame` 合并广播：手机端高频广播时只保留最新一份。

3 个 Tab 切换：

- **项目排名**：当前选中项目的总排名（接力赛自动 swap 姓名 / 代表队列）
- **团体积分**：各代表队总分 / 金 / 银 / 铜
- **纪录榜**：当前赛事所有纪录列表

### 22.3 大屏顶部主纪录条

显示当前项目的两条纪录：左为"主纪录"（WR/AR/NR/省R/市R 之一，由主控配置），右为"赛会纪录 CR"。

---

## 23. 在线报名 / 注册工具

### 23.1 `register.html`（在线报名）

启动连接遮罩输入 IP → 进入主界面：

- **个人项目报名**：姓名 / 性别 / 出生日期 / 身份证 / 代表队 / 单位简称 / 组别 / 协会注册号 / 备注 / + 多项目报名
- **接力队报名**：队名 / 性别 / 组别 / 单位简称 / 项目 / 报名成绩 / 4 棒队员（含出生日期）

**组别 / 项目 / 性别下拉**：从主服务器实时同步（`比赛参数设置管理`修改后立即生效）。

#### 23.1.1 多人报名工作流

```
[录入 1 人表单] → [+ 添加到报名列表] → [清空表单 → 录入下一人] → … → [全部提交报名（N 人）]
                              ↓                                                   ↓
                  待提交报名列表（绿色卡片，可"修改"/"删除"）         REGISTER_SWIMMERS_MULTI
                                                                              ↓
                                                            主服务器逐条校验
                                                                              ↓
                                                                ┌─ 全部通过 → 写入 + 弹窗成功
                                                                └─ 任一未通过 → 整批不入库 + 弹窗
                                                                                列出原因 + 失败条目变红
```

#### 23.1.2 查询 / 修改已提交的报名

黄色卡片"查询 / 修改我已经提交的报名"：输入参赛号或（姓名+代表队）查询；服务器返回当前已注册数据，回填到表单进入"修改模式"，再次"添加到报名列表 → 全部提交报名"会以 `isResubmit=true` 提交，服务器先清旧记录再写新记录。

---

## 24. 自动化测试机器人

`test_bot.html`：

- **启动连接遮罩**：必须先输入主服务器 IP 并连接成功才解锁操作面板
- 一键模拟报名（个人 / 接力）100+ 运动员
- 模拟比赛流程（准备就绪 → M.发令 → 触板序列 → 确认本组成绩 → 切到下一组 → 晋级…）
- 接力比赛专项：4×100 / 4×200 / 4×100混合泳，每棒触板与出发台动作均按真实顺序模拟
- 可在没有硬件、没有真人时，演示 / 压测整套系统

---

## 25. 硬件参数调试机器人

`ParamDebugBot.exe`：

- **启动连接遮罩**：WinForms 半透明遮罩盖住下方面板，必须先按"连接 / 启动监听"成功后才解锁
- 不连主服务器，直接对接硬件计时器（UDP / TCP / 串口）
- 双向调试 12 字节帧
- 实时显示每条命令的子码 / 数据 / 时间戳

适合验证 §4 协议变更后硬件固件的实现是否正确。

---

## 26. 常见问题

### 26.1 主服务器闪退或无法启动

- 查看是否被防病毒软件拦截
- 检查 .NET Framework 4.5+ 是否安装
- 端口 3002 是否被其它进程占用

### 26.2 客户端连不上主服务器

- 检查 IP 是否正确（用 `ipconfig` 确认主机 IP）
- 防火墙是否放行 `SwimmingScoreboard.exe`
- 主服务器右下角"连接状态"是否显示已启动 WebSocket
- 浏览器端启动遮罩里的 IP 输入框可重输；按 3 次失败后会弹 confirm 询问是否换地址

### 26.3 顶部"硬件计时器"一直红色

- 主服务器：检查"系统工作状态 → 硬件计时器连接"是否选好协议（TCP/UDP/串口）并按了连接
- EXE 远程台：顶栏自身的硬件指示灯是 EXE 直连硬件的状态；如果只走主服务器代理，这里也会亮
- 已连接但红？看主控日志窗口是否有"超时"提示——硬件可能掉线，可点"网络连接"重连

### 26.4 标 DSQ 后该道触板/盲表不工作

- 已修复：DSQ 道继续接收原始数据（触板 / 盲表 / 分段 / 反应时全部继续记录）；仅大屏排名 / 最终成绩排除该道。如果还是不工作，确认 EXE 是 v2026.05.20+ 版本

### 26.5 道次顺序改成"逆序"后硬件道次发错

- 已修复（v2026.05.18 6081107）：所有 PC→硬件帧的 lane 字段都按"屏幕位置"编码，逆序时 `hwIndex = 9 − lane`
- 若仍异常，确认主服务器和 EXE 都升到 v2026.05.20+，并重启硬件让 0x65 RefreshDisplay 把状态对齐

### 26.6 一组多人破纪录但大屏只显示第 1 名的标识

- 这是 FINA 标准行为（`EnforceOnlyLeaderRecordNote`）。第 1 名（含 5 ms 内并列）保留 RecordNote，其余清空

### 26.7 已确认成绩消失

- 检查"赛程导航"是否仍显示 `[已完赛]`
- 在"成绩与排名" Tab 检查组次下拉是否包含该组
- 查看主控日志窗口是否有"赛程导航刷新失败 / 成绩与排名刷新失败"红色错误

### 26.8 接力打印反应时少于 4 棒

- 服务器按棒次索引覆盖写入 `LegReactionTimes`，槽位数固定 = 项目名 N×D 的 N
- 未触发硬件事件的棒次显示 `—`

### 26.9 ScheduleEditor 启动很慢 / 一直显示"未连接"

- 编排端不开 3002 端口（不是 WebSocket 服务器），需要连主服务器
- 点顶栏 "EditorSync" 工具条内的"连接"按钮，输入主服务器 IP
- 主服务器必须先启动，编排端才能拉到 CompetitionPackage

### 26.10 编排端改成绩，主服务器看不到

- 编排端 `AutoSaveData` 时通过 `EDITOR_PUSH_PACKAGE` 整包推主服务器，主服务器再 broadcast 给其它编排端
- 若网络抖动可能丢包；重启编排端会重新拉一次完整 Package

### 26.11 设备测试期间硬件没有响应

- 设备测试发送 `0x1D` 给硬件；如果硬件固件不识别 0x1D（旧固件），仅 PC 端进入测试模式，硬件不做任何配合，事件链路可能失效
- 升级硬件固件到 v2026.05.20+ 解决

### 26.12 电池电压一直显示 "—"

- 硬件需主动上报 `0x4B BatteryVoltage`（连接后会立即报一次，电压变化时也会报）
- 30 秒无新报会清空显示
- 旧固件不支持 0x4B 时永远 "—"，升级固件解决

### 26.13 大屏卡顿

- 已优化为分区刷新（100 ms 滚动时间 + 250 ms 廉价字段刷新 + DOM 缓存）
- 老版本浏览器（IE / 旧 Edge）渲染慢，建议 Chrome / 新 Edge

### 26.14 自动分组结果不符合 FINA 规则

- 请确认"项目类型"识别正确（短距 50/100/200 / 中长距离 400+ / 接力）
- 重新点 **预赛自动分组**

### 26.15 接力队员重复登记

- 已修复：相同 (队名 + 性别 + 项目) 的接力队会被识别为更新，不会增加重复行

### 26.16 计时复位后大屏没切到欢迎画面

- 仅在"本赛次最后一组已确认 + 计时复位"才会切欢迎画面
- 中间组的清零会自动切到下一组（仍显示比赛视图）

---

## 附录 A：CSV 表头速查

| 文件 | 表头（首行） |
|---|---|
| **运动员报名** | `号码,姓名,性别,代表队,项目,报名成绩,年龄,出生日期,身份证号,电话,备注,组别,单位简称` |
| **比赛项目表** | `比赛项目` |
| **组别表** | `组别名称` |
| **性别表** | `性别` |
| **赛次表** | `赛次` |
| **组数表** | `组数` |
| **代表队号码段** | `代表队,起始号,结束号,位宽` |
| **赛程编排** | Excel 工作簿 (.xlsx)，含项目 / 组别 / 组次 / 道次 / 姓名 / 代表队 / 报名成绩 列 |

---

## 附录 B：持久化文件清单

所有 JSON 都在各 EXE 同目录，便于备份与迁移。

### B.1 通用

| 文件 | 写入方 | 内容 |
|---|---|---|
| `last_competition.txt` | 主服务器 / ScheduleEditor | 上次打开的赛事名（用于下次启动自动加载） |
| `Database/<赛事名>.json` | 主服务器 / ScheduleEditor | CompetitionPackage 整包（运动员 / 接力队 / 纪录 / 团体计分 / 赛程 / 5 维元数据 / LaneCloseSettings / ScoringConfig / ProgramBook / ResultBook / DisplayRecordOptions / ConfirmedHeats） |
| `Database/RawData/<组别_>性别_项目_赛次_第N组.txt + .html` | 主服务器 | 每场比赛"原始数据"快照——出场名单 / 原始事件流 / 最终成绩 / 分段明细 / 备用成绩 |

### B.2 主服务器专属

| 文件 | 内容 |
|---|---|
| `timing_settings.json` | LaneCloseSettings：泳道关闭时间 / 出发台关闭延迟 / 成绩确认延迟 / 抢跳阈值 / 分段显示时长 / 盲表代替延迟 / 第 1 名停留 / 大屏翻屏 / 终点位置 / 起点位置 / 反应时启用 / **道次顺序 LaneOrder** / 左右盲表数量 |
| `device_states.json` | 每道设备配置数组：`leftTouchpadBroken` / `rightTouchpadBroken` / 出发台 / 盲表 1-3 × 左右 × `Broken` + `NotInstalled` / `leftManualEnabled` / `rightManualEnabled`。**用户在"设备状态管理" / "手动按键管理"对话框确定后即写入**，每次启动 / 加载赛事 / 改泳道数后通过 `ApplyPersistedDeviceStates()` 恢复 |
| `timing_connection.json` | 硬件连接配置：`LastType`（"serial"/"tcp"/"udp"）/ `SerialPort` / `SerialBaudRate` / `TcpHost` / `TcpPort` / `UdpListenPort` / `UdpSendHost` / `UdpSendPort` / `AutoReconnectOnStartup` |

### B.3 各桌面 EXE 凭据

| EXE | 凭据 | 记住 |
|---|---|---|
| RemoteTimingControl | `timing_credentials.json` | `timing_remember.json` |
| RegistrationTool | `register_credentials.json` | `register_remember.json` |
| RemoteDisplayControl | `display_credentials.json` | `display_remember.json` |
| ScheduleEditor | `editor_credentials.json` | `editor_remember.json` |

格式：

- 凭据 = `{ user, salt(hex), hash(hex) }`，hash = SHA256(salt + password)
- 记住 = `{ Username, EncryptedPassword(Base64-DPAPI) }`，DPAPI 用 `DataProtectionScope.CurrentUser` 加密；换 Windows 账户读不到

---

## 附录 C：版本变更摘要

### v2026.05.20 — 协议大改 + 套件化（本版）

- **新增硬件协议命令 6 条**：0x60 Set_LaneEnableDisable / 0x61 Set_LapRemaining / 0x62 Set_LaneOrder（双向）/ 0x63 Set_FinishPosition（双向）/ 0x64 Set_TimingsBundle / 0x65 Set_RefreshDisplay
- **扩展为双向 2 条**：0x3A Set_PoolSingleOrDoubleTP / 0x47 Set_LaneOpenClose
- **新增 0x44**：作为 0x40 PoolConfig 的别名
- **lane 索引按屏幕位置编码**：逆序模式下 `hwIndex = 9 − lane`，PC→硬件帧统一通过 `LaneToHwIndex` 映射
- **周期校时**：连接后立即 0x39 + 每 30 分钟刷一次
- **道次顺序 / 终点位置 / 泳池触板**：参数设置 3 个单选下发硬件，硬件 net_test 改动后上报回 PC
- **盲表代替成绩延迟**：单位 0.1 秒 → **整数秒**（0–9），与 0x64 D6 对齐
- **参数设置进出保存恢复**：进入快照 IsFinished / LeftLapManualAdjust / RightLapManualAdjust；OK 后恢复，避免误清零
- **0x65 RefreshDisplay**：参数对话框 OK 后下发，硬件主控按新参数重画不丢比赛状态
- **顶部硬件计时器连接状态指示**：主服务器 + EXE 远程台顶栏新增红/绿圆点 + "已连接 / 未连接"
- **按钮命名统一**：就位 → 准备就绪 / 发令 → M.发令 / 复位 → 计时复位
- **系统硬件栏**：主服务器右栏 + EXE / HTML 顶 nav，3 控件（网络连接 / 设备测试 / 设备全开 + 电池电压显示）
- **设备测试模式**：发 0x1D 给硬件，所有非损坏设备 Open，硬件来什么数据都接收显示，再点退出
- **道次打开 / 关闭 按钮**：泳道操作 2×5 网格新增两键，单道 0x60 启用 / 屏蔽
- **设备状态持久化**：新增 `device_states.json` 保存触板 / 盲表 / 出发台损坏 + 未安装 + 手动键启用
- **电池电压实时显示**：硬件 0x4B 上报后 EXE / 主服务器右栏显示（绿/黄/红，阈值按 7.4 V 包对齐）
- **TimingConnection 持久化**：新增 `timing_connection.json`，硬件连接配置跨赛事保留
- **ScheduleEditor.exe**（新）：编排员 / 成绩员 / 文档员协同工位，通过 EditorSyncClient 与主服务器整包同步 CompetitionPackage
- **四个桌面 EXE 套件化登录**：RemoteTimingControl / RegistrationTool / RemoteDisplayControl / ScheduleEditor 统一深色 Suite 风格登录窗 + DPAPI 记住密码

### v2026.05.10

- **多人在线报名**：register.html 新增"待提交报名列表"工作流；`REGISTER_SWIMMERS_MULTI` 一次提交；全部通过 / 失败弹窗反馈
- **起跳犯规改为人工判罚**：硬件检测的"抢跳"只置 `IsSuspectFalseStart`（反应时标红），是否 DSQ 由裁判按按钮决定
- **破 / 平纪录自动比对**：完赛即写 `LaneResult.RecordNote`（WR/OR/AR/AJR/NR/省R/市R/CR），三端 + 大屏 + 打印同步显示；裁判确认后 `UpdateRecordsAfterConfirm` 自动刷新对应 `SwimmingRecord`
- **并列名次 1-2-2-4 规则**：`UpdateHeatRanking` / `ComputeLiveRanks` / `GetEventRanking` 三处统一
- **圈数 ▲▼ spinner**：每道左右独立调整，钳到 [0, 该侧实际触板次数]
- **反应时(RT) 开 / 关**：参数设置中新增；关闭后所有反应时处理跳过
- **大屏翻屏时间**：参数设置中新增（默认 5 秒）
- **接力出生日期**：三个报名端每棒新增"出生日期"字段；`RelayLeg.SwimmerBirthDate` 持久化贯穿
- **接力 4 棒反应时全链路**：服务器按棒次索引覆盖写入 `LegReactionTimes[legIdx]`，打印 / ResultGrid / 原始数据全部按 N 棒展开
- **大屏反应时移到备注栏**：成绩栏不再显示反应时；备注栏优先级 DSQ/DNS/DNF/DQ > 破纪录 > 反应时
- **DSQ 抢跳道继续比赛**：标 DSQ 后泳道仍处于"接收态"，仅大屏排名 / 最终成绩排除该道
- **本组只第 1 名保留破 / 平纪录**：`EnforceOnlyLeaderRecordNote` 强制
- **服务器广播去掉 notes**：避免运动员个人备注泄露到大屏
- **启动连接遮罩推广**：display.html / leaderboard.html / test_bot.html / ParamDebugBot 全部加入
- **解决方案 x64 配置**：VS 2015 Format 12.00，加入 `Debug|x64` / `Release|x64` 平台配置

### v2026.04.29 — 大屏分区刷新优化

- display.html 不再 500 ms 整屏 innerHTML 重建；100 ms 滚动时间 ticker + 250 ms 字段对比刷新 + DOM 缓存复用

### v2026.04.28 — 计时控制三端一致

- 引入 `_confirmedHeats` 集合永久锁定已确认组次，并入档持久化
- "计时器清零"按是否已确认拆两种语义；最后一组完赛 → 自动清空泳道 + 大屏切欢迎画面
- 主控 / EXE / HTML 三端共用同一守卫与提示规则

### v2026.04.27 — FINA SW 3 自动分组 + 三端组别同步

- 重写 `HeatScheduler.GenerateHeats / GenerateRelayHeats` 严格遵循 FINA SW 3.1.1.4 / 3.1.1.5 / 3.1.2
- 自动日程晚上场起始时间 19:00 → 17:30
- `比赛参数设置管理` 任意修改通过 `NotifyMetadataChanged` 同步到 WPF 下拉 + 网页报名端

### v2026.04 早期更新

- 增加"代表队简称"列；修复显示
- 网页报名端动态从服务器同步组别 / 项目下拉
- "成绩与排名"组次下拉只列已确认组
- 删除赛事 / 清库时同步刷新所有面板
- "比赛类型管理" 改名为「比赛参数设置管理」；「名次分设置」按钮从"成绩与排名"工具栏移入
- 录取标志 Q（预赛 / 半决赛打印备注栏绿色加粗）
- 分段计时报告合并表
- 团体计分名次分可配置 `ScoringConfig`
- 并列名次按硬件 1/100 秒整数判定（`IsTieTime`）

---

**主服务器版本号**：v2026.05.19
**手册版本**：v2026.05.20
**最后更新**：2026-05-20
